# 更新日志

本项目遵循「版本号体现在功能里程碑」的惯例。当前版本 **v2.30**。

---

## v2.30 — LLM 返回安全解析统一抽象（json_guard）

抖音"Agent 防循环 4 条"里唯一对智论助手真有价值的那条——**工具返回合法性校验**——
本轮收敛到一个统一抽象，**不动行为，只收口兜底逻辑**：

- 新增 `agents/json_guard.safe_parse_json(text, *, expect, required) -> (value, status, raw_len)`
  - 三层降级：直接 parse → markdown 代码块提取 → inline 反引号提取 → 首段 `{}`/`[]` 提取
  - 结构校验：顶层类型 + object 必填字段
  - 超长截断：`> 200 KB` 直接判 `too_long`（防 LLM 异常长输出撑爆内存）
  - 失败静默：返 `(None, reason_code, raw_len)`，绝不把 `JSONDecodeError` 原文透出去
- `llm_review._parse_review` 改用 `safe_parse_json` 替换原手写双层兜底；旧的"两段 try/except
  + re.search []" 是 2026-09 v0.5.1 留下的，3 个状态码现在收成 1 个统一失败路径
- 新增 `json_guard_test.py`：**21/0 全过**（顶层类型 4、必填字段 3、markdown 3、inline 2、
  前后废话 2、空/超长/坏 JSON 4、与旧 llm_review 格式兼容 2、异常路径 1）
- `desktop.spec` 的 `('agents','agents')` 整体打包已覆盖新模块；`build_desktop_test`
  预检 **42/0 全过**

不动什么
- **不做 chat 流熔断**——智论助手没有 ReAct chat 循环：`/api/audit_chat` 是单轮解释，
  `llm_enhance/llm_review` 也都是单次 `router.complete()`。本来就没有循环，加熔断是空对空。
- **不做"重复任务去重"**——流水线性调用，重入风险低。
- **不做 Prompt 约束熔断**——初级开发者手段，资深工程方案已替代。
- **不做"降低 AI 率 / 小红书妙招"**——违背红线 #4「不做代写、不绕检测」。

测试：单跑 21/0；build_desktop 预检 42/0；回归脚本中 json_guard 被自动发现套件（见下条）。

---

## v2.29 — 材料整改落地：真离线 + 逐项发送提示 + 付费熔断 + 密钥守门

参赛材料《产品说明-桌面端》《本次修订说明与待补材料》列了三项"仍待实现"的软件短板，
本轮逐项收口（均 L0/L1 小改，不碰计算层）：

| 待补材料原文 | 修法 |
| --- | --- |
| 「当前页面引用外部 Markdown 渲染脚本，'本地计算'不等于'全部功能完全离线'」 | markdown-it 14.1.0 下载进 `static/vendor/` 随包分发（`desktop.spec` 的 `('static','static')` 整目录打包，spec 零改动），页面不再依赖 CDN——**断网时统计结果照常渲染**。SW 缓存策略天然兼容（静态资源 stale-while-revalidate） |
| 「界面数据发送说明…软件内逐项发送提示仍待统一」 | 三个会出网的功能补齐**逐项提示**：✨AI 深度解读注明"发送算好的统计摘要，不含原始数据表"；🔬AI 深度审计注明"发送论文开头约 2000 字 + 统计量摘要"；🗣审计追问注明发送比对摘要与问题；🖼图表核查补充"发送所选图片与结论句" |
| 「软件自动预算上限尚需实现验证」 | 新增**付费调用熔断** `LLM_PAID_MAX_CALLS`：进程内"付费模型 × 项目方 Key"调用达到上限即跳过付费候选（报错标注"付费熔断"）。BYOK 用户自付不计数；免费模型无现金成本不拦；先记账再调用（失败也占额度）；`/api/llm_stats` 新增 `paid_brake` 观测字段 |
| 开源仓库密钥防护只有纪律、没有强制检查 | 新增 `scripts/secrets_scan.py`：对已跟踪文件扫 sk-*/GLM/PAT/阿里云 AK 形态 + `.env` 误跟踪；测试夹具走"路径+字面量"双确认白名单。接入 CI 首步（发现即红） |

测试：新增 `paid_brake_test.py`（17 断言：默认不熔断 / 超限跳过 / BYOK 不计 /
失败也记账 / 50 线程并发计数不丢 / 非法上限容错）。密钥扫描对本仓 176 个
已跟踪条目实测通过（仅合成夹具命中，均在白名单内）。

---

## v2.28 — 防护网整备：测试自动发现 + 探针基准清单（外部扫描报告修复）

第三方对 v2.27 快照的全仓扫描指出防护网层三处问题，逐项修复：

| # | 问题（扫描报告） | 修法 |
| --- | --- | --- |
| P0-1 | `stream_test.py` 连打 11 次 /api/analyze 吃 429 → StopIteration 崩溃，且不在回归清单里（下一次推 CI 必红） | 按硬性纪律 7 关限流；`regress_run.py` **改自动发现**后再也不会漏套件 |
| P1-1 | 16 个套件会打 LLM 限流路径却未关限流（跨套件顺序敏感 429） | 11 个 in-process 套件补 `RATE_LIMIT_DISABLE=1`（user_accounts_test 例外——它专门测限流且配额自管；methods/smoke/regression 为 HTTP 外部服务型，归入 ENV_DEPENDENT 由服务端控制） |
| P1-2 | `_syntaxcheck/` 两个关键探针（defense_export 82 断言 / image_audit 47 断言）历史上两次丢失 | **按当前源码行为重建**：defense_export_probe 20 断言（cdFilename UTF-8 优先契约 9 例 + Werkzeug 残缺回退名事故场景 + 双导出点守门 + 后端 download_name）；image_audit_probe 35 断言（BYOK 透传 + 重选同文件修复 + 拖拽 + 错误路径 + 后端 5MB/白名单）。断言数与 v2.23 版本不必相同，契约不变 |
| P1-2 保险丝 | CI「探针数 < 3 报警」阈值太低，丢一半照样绿灯 | 改**基准清单逐个点名**（8 个探针缺一即红）+ 总数少于清单即红 |
| P1-3 | 回归清单漂移：仓库 59 个测试文件 / 手工清单 26 / README 三个数字互相打架 | `regress_run.py` 改**自动发现**（与 CI 同规则：排除 4 个 live + 4 个 HTTP 服务型），README 徽标与正文同步 |
| 文档 | 安全自查报告 §4 的两条"已知局限"（口令爆破/按人限额）已被 v2.27 修掉但报告未更新 | §4 加更新注记，逐条标注修复版本，不再自相矛盾 |

验证：8 个前端探针全过（含重建的 2 个，55 条新断言）；全量回归自动发现
**55 套件**全部通过；网页端真机冒烟（含账号全链路）通过；exe 重建后 e2e 12/12。

---

## v2.27 — 独立账号系统：每人一个账号，逐项收口安全自查的"已知局限"

新模块 `user_accounts.py`（`ACCOUNTS_ENABLED=1` 开启；**不设 = 完全关闭**，
桌面端/单机/既有部署一个字节不变）。口令门禁仍是"看门人"，账号层在门禁之内认人：

| 安全自查原局限 | v2.27 解法 |
| --- | --- |
| 限额按 IP 不按人（校园网 NAT 互挤） | 限流 key 改 `user:<名>`，**每人独立配额**；未登录/未开启退回按 IP |
| `file_id` 无鉴权拿到即读 | 上传归属登记 `_SESSION_OWNER`；钩子中央校验，他人 file_id → **403**（与数据同生命周期，过期/淘汰一并清除） |
| `/unlock` 无独立账号锁定 | `security_guard.LoginLockout`：/unlock、/login、/register 共享按 IP 锁定（**5 次失败锁 15 分钟**，锁定期内正确口令同样被拒；内存有界，与限流器同一纪律） |
| 依赖未锁版本（供应链） | CI 新增 `deps-audit` job：pip-audit 扫 requirements.txt 已知漏洞（报告制，稳定后再升门槛） |
| `/s/` 分享在门禁外 | **保留**（设计如此：给没账号的导师看，96bit 不可猜）；账号层同样豁免并写明 |

账号设计要点（与项目红线一致）：

- **邀请制注册**：注册表单必须填与门禁同一把口令——光知道网址的人不能注册；
- 口令存 **PBKDF2（12 万轮 + 随机盐）摘要**，校验常量时间比较，未知用户名做等价哈希抹平时序差；
- 会话 cookie 为「用户名｜过期｜HMAC 签名」，**签名密钥由口令派生——改口令全员账号会话立即失效**；
- **数据仍全内存不落盘**；只有账号索引（摘要，无任何用户数据）落 `.accounts/accounts.json`（已 gitignore），否则每次重启全员重注册；
- 分层顺序：门禁（unlock）→ 账号（login/register）→ 按人限流 → 业务。

测试：新增 `user_accounts_test.py` **30 条断言**（分层顺序、邀请制、
归属 403、按人限流、防爆破锁定、cookie 篡改、改口令失效、退出、关闭模式零干扰），
登记进回归清单。全量回归 27 套件全绿；网页端真机冒烟通过（注册→登录→按人
限额→跨账号 403 全链路）。

---

## v2.26 — 安全自查落地：内存有界化 + 限流补登 + 向导完整性（含 v2.25 合并入库）

安全自查（见 `安全自查报告.md`）之后的落地批次；v2.25 的 BYOK 隔离修复与
本轮改动同在工作树，一并入库。网页端真实冒烟暴露出 1 个致命笔误，已修并加测试锁死。

| # | 问题 | 修法 |
| --- | --- | --- |
| ① | **会话表 `_SESSION` 从不清理**：多用户部署下每次上传的 DataFrame 永不释放（确定性内存泄漏） | `_session_put` 统一入口：空闲超 4h 回收、总量超 64 份按最久未写入淘汰；`upload / local_open / simulate` 三个写入口全部收编 |
| ② | **`_session_put` 首版实现把收尾一行写成递归调用自己**——任何上传直接 RecursionError、`/api/upload` 全灭（单测 8 套件连环红；单元测试全绿但真实服务一跑就崩，再次证明"跑一跑"不可省） | 改回 `_SESSION[file_id] = df`；新增 `session_bound_test.py` 9 条断言（上传可用 / TTL 回收 / 超限淘汰 / 图表缓存 LRU 上限）并登记进 `scripts/regress_run.py` |
| ③ | **图表缓存无上限** | `_chart_cache_put` LRU 上限 128 |
| ④ | **`/api/analyze` 漏登 LLM 限流桶**：`use_llm=1` 真烧 token 却只受 60/分普通桶保护 | `security_guard.LLM_PATH_PREFIXES` 补登 `/api/analyze`；registry_test / wizard_test 按 `security_guard.disabled()` 约定显式关限流 |
| ⑤ | audit 两处统计边界：单样本 SD（std=NaN 与任何值比较都是 False → 被静默判"一致"）；合并 SD 除零（两组各 1 样本时 n1+n2-2=0，整份核查报告 500） | 样本不足 2 个跳过 SD 比对；合并 SD 前检查自由度 > 0 |
| ⑥ | `/api/analyze` 的 BYOK 注入在 stream 分流之后——流式路径（前端默认）完全拿不到用户 Key | 注入移到分流之前，双路径不再漂移 |
| ⑦ | **前端向导函数嵌套吞并事故（2026-09-18）**：重复 `function updateWizard()` 声明把 validateMethod / methodDesc / setStep4 等吞成嵌套函数，语法完全合法、`node --check` 抓不到，运行时主流程按钮永久禁用 | 去重修复；新增 `_syntaxcheck/wizard_integrity_probe.js` 两道防线（内联脚本 new Function 编译 + 向导函数族全文件唯一声明） |

验证：全量回归 26 套件全绿（新增 session_bound_test）；网页端冒烟 10/10
（门禁三态·中文口令 HMAC cookie / 上传 / 分析 / LLM 桶第 9 次起 429 + Retry-After）；
桌面端 exe 重建 + `scripts/exe_e2e.py` 通过。

---

## v2.25 — 安全修复：BYOK 用户 Key 跨用户泄露（请求级隔离）

全仓安全扫描发现 1 个真实漏洞（多用户部署场景），本轮修复：

| # | 问题 | 修法 |
| --- | --- | --- |
| ① | **BYOK 用户 Key 残留在进程级单例 Router 里**：用户 A 填的自带 Key 写进普通 dict 后永久残留，用户 B 之后的请求会被路由到 A 的 Key 上（串号 + 盗用 A 的额度；A 若是付费 Key 就是真金白银），且带用户 Key 的 Agent 还进共享缓存加速扩散 | `agents/router.py`：`_user_keys` 改 **contextvars 请求级隔离**（每请求线程各一份，请求结束零残留）；带用户 Key 的 Agent **不进共享缓存、不污染 `_resolved`**；`_resolved` 捷径加「当前请求有该平台用户 Key 必须绕开缓存」防护 |
| ② | 前端 `/api/audit_chat`、`/api/audit_image` 不带 `collectByok()`（原先靠服务端残留"碰巧能用"，隔离后会静默失效） | 两个入口补透传；`_syntaxcheck/byok_fields_probe.js` 新增 4 个 LLM 入口透传点契约断言 |

测试：`byok_test.py` 新增 §9 共 7 条隔离断言（含多线程模拟另一用户请求）；
byok 49/0、kimi_mimo 41/0、router_cooldown 46/0、security_guard 56/0、
secrets_guard 49/0，全量回归绿。扫描全貌见 `安全自查报告.md`。

---

## v2.24 — 首推 CI 四轮排障：四矩阵全绿

GitHub 首推后 Actions 红，四轮排障（每轮根因都进了测试或注释）：

| 轮 | 根因 | 修法 |
| --- | --- | --- |
| ① | workflow env 同时写 `NO_PROXY` 与 `no_proxy`——GitHub 对 env 键大小写不敏感去重 → **startup failure**（run 红且无任何 job、日志 404） | 只留小写一份 |
| ② | 测试硬编码本机路径（`D:\论文排版辅助agent`、`.venv/Scripts/python.exe`、本机 node.exe）+ 依赖 gitignore 的 `模板论文/` | 路径全部改 `os.path.join(ROOT, ...)` / `sys.executable` / PATH 上的 node；模板论文套件 fail-soft SKIP |
| ③ | `paper_writer._render_latex` 的 f-string 表达式内含 `r"\_"`——**Python 3.10 是 SyntaxError**（3.12 正常，故 ubuntu 3.10 矩阵单独红） | 表达式挪出 f-string（`conclusion_tex` 变量） |
| ④ | CI Windows 的 git autocrlf 把库内 .md checkout 成 CRLF；CLI（raw bytes）与网页端（文本模式）读同一文件产出不同文本，表格渲染 `replace("
")` 漏 CR——cli_test「CLI == 网页端」**等长不等值**（2348==2348） | `read_paper_text` 全部读取分支出口 `_normalize_newlines`（CRLF/CR→LF）；audit 表格渲染补 `replace("
")`；cli_test 加 unified_diff 诊断探针（本轮定位功臣） |

最终：**ubuntu/windows × 3.10/3.12 四矩阵全绿**。附带收益：CRLF 论文文件的上下文摘要不再残留 CR（产品级修复）。

---

## v2.23 — 答辩包 / Word 导出前端加固（中文文件名曾被删光）

`index.html` 有 170KB 内联 JS，已有探针只盖住 4 个模块。答辩包与导出这一整段
（约 330 行）**一行断言都没有**——偏偏它是「最后一公里」：红线守门的出口、
用户唯一会带走的文件。写完 `defense_export_probe.js`（82 项）后修掉 2 个真缺陷：

| # | 问题 | 修法 |
| --- | --- | --- |
| ① | **导出的 Word 文件名里中文被删光** | 新增 `cdFilename()`：优先 RFC 5987 的 `filename*`（并 URL 解码），取不到才退回 ASCII 的 `filename=`，最后才是前端兜底名 |
| ② | 打包图表 zip 的下载锚点没挂到 document | 补 `document.body.appendChild(a)` + `a.remove()`，与导出 Word 那条路一致 |

① 值得展开说。后端 `send_file(download_name="智论助手_xxx.docx")`，Werkzeug 对
非 ASCII 文件名会同时给两个参数，实测响应头是：

```
attachment; filename=_independent_t_1234567890.docx; filename*=UTF-8''%E6%99%BA%E8%AE%BA%E5%8A%A9%E6%89%8B_...
```

`filename=` 那个是 **ASCII 回退**，`智论助手` 四个字被**整段删掉**，只剩前导下划线；
真名只在 `filename*` 里。而前端正则 `/filename[^;=\n]*=/` 命中的正是残缺那个 →
用户下载到的每个 Word 都叫 `_independent_t_1757xxxxx.docx`，看不出是什么报告。
探针里留了**反向守卫**：断言旧正则确实只会拿到 `_independent_t_...`，别让人改回去。

**顺带修掉一个会让 CI 挂死的测试缺陷。** 并发会话给启动器加了系统托盘后，
`desktop_launcher_test.py` 的「新起实例」分支会真的走进 `_try_tray` ——
它用 pystray 的 `icon.run()` 占住**主线程**（Windows 托盘的要求），
于是整个回归卡死 12 分钟没有任何输出。修法是给 `_try_tray` 打桩，
并补一条新契约断言：**托盘接管时不再调 `app.run`**（两套主循环会打架）。
同一处还补了 stdin 重定向：`_wait_for_enter()` 里的 `input()` 读的是 stdin
不是 stdout，后台批处理里 stdin 是空管道时会永久阻塞
（`contextlib` 只有 `redirect_stdout/stderr`，没有 stdin 的，得自己换 `sys.stdin`）。

顺带补上 `prefix_cache_test.py` 缺的 `--live` 开关（与 `kimi_mimo_live_test.py`
同一约定）：它会真调 DeepSeek / 智谱 / 百炼，项目 `.env` 里有 Key 时会被
`for f in *_test.py` 扫进去，**7 分钟零输出**把整轮回归拖死。
CI 的 `LIVE_SUITES` 黑名单只救得了 CI，救不了本地全量——脚本自己不长开关，
黑名单一漏就是又一次"回归跑到一半不动了"。

⚠️ 另需记录：本轮收尾时发现 `_syntaxcheck/` **被整目录清空**（只剩一个
`datacheck_probe.js`），`simulate` / `review_share` / `plugin_methods` 三个探针
**源码已不可恢复**。本轮的 `defense_export_probe.js`(82) 与 `image_audit_probe.js`(47)
已按源码 / 断言清单重建。`_syntaxcheck/` 是各会话共享目录，且未纳入 git ——
**不要整体清理**。CI 里新加的"探针数 < 3 报警"就是为这种情况准备的。

**顺带：v2.21 那道打包预检守卫第一次真的拦到了人。** 本轮回归跑到一半时
`build_desktop_test` 红了——不是我的改动，是并发会话新增了 `version.py`
（`desktop_launcher.py` 引用它）却没同步 `desktop.spec`。预检当场报：

```
[预检失败] desktop.spec 的 datas 清单缺少以下本地模块：
    - version                     (被 desktop_launcher.py 引用)
```

已补 datas + hiddenimports（26 → 28 个模块）。**这就是把守卫接进日常回归的意义**：
它会在别人手滑的当天报警，而不是等到用户双击 exe 闪退才发现。

探针覆盖：守门（未签学术诚信承诺 → **一个字节都不许发出去**，防失败开放）、
请求体契约（`include_datacheck=true`——答辩前必须过数据体检）、三态渲染、
`X-Missing-Charts` 部分失败提示、XSS 转义、以及**失败路径按钮必须复原**
（导出按钮一旦永久禁用，用户就再也拿不到报告了）。

---

## v2.22 — 读图核查：重选同一张图会哑火

`ImageAudit`（多模态读图核查）是唯一一个完整 IIFE 却没有探针的模块。补
`image_audit_probe.js`（47 项）后暴露一处真缺陷：

`change` 事件里没清 `inp.value`。浏览器对 `<input type=file>` 的约定是：
**选中同一个文件不会再触发 `change`**。于是用户先选错一张、再选同一张，
界面毫无反应——换张图正常、重选就哑火，属于最难自查的那类 bug。
修法是 `pick(f)` 之后 `inp.value = ''`（`f` 已先取出，清空 files 不影响引用）。

顺带补一条**反向守卫**：清空 `value` 是为了「能重选」，不等于「把用户已选中的图弄丢」——
选中一个坏文件后，上一张有效图必须还在、按钮仍可用。

## v2.21 — 打包清单补漏（`review_share` 会让 exe 闪退）

打包脚本不是产品，但**打出来的 exe 是**。2026-09-11 出过真事故：spec 的 datas
漏了十几个模块，93.9MB 的 exe **双击即闪退**。事后加了 `preflight()` 预检——
**但没人跑它**。实测：v2.16 引入的 `review_share` 一直没进清单，也就是说
**在此之前 `desktop.spec` 打出来的 exe 是坏的**：

```
[预检失败] desktop.spec 的 datas 清单缺少以下本地模块：
    - review_share                (被 app.py 引用)
```

- 修：`desktop.spec` 的 datas 与 hiddenimports 双双补上 `review_share`；
  预检从 rc=2 变为 rc=0（26 个本地模块均已声明）。
- 顺带删掉 `_SKIP_NAMES` 里过期的 `simulate` 豁免 —— 它早已随包，
  留着只会让"哪天它被从清单里删掉"不再报警，正好是这道检查要防的。
- 新增 **`build_desktop_test.py`（42 项）**，核心价值一句话：
  **把"记得回来改 spec"变成"CI 会红"**。断言 `preflight() == 0`，
  以后任何被 app 引用的新模块忘了同步 spec，全量回归就会红。
  反向护栏：注入一个假模块后预检必须 rc=2、点名模块、说明"闪退"后果并给出修法示例。
  测试套件 56 → **57**。

---

## v2.20 — 插件沙箱加固（stdout 是协议的，不是插件的）

`plugin_worker.py`（65 行）是插件沙箱的子进程入口，也是**外部代码与主程序之间
唯一的墙**，却零测试。读 `plugin_registry._run_subprocess` 时发现：父进程拿的是
`pickle.loads(proc.stdout)` —— **插件往 stdout 写的任何东西都会混进 pickle 流**。

值得如实记录的是：不 flush 的小 `print` 会**侥幸**通过（文本层缓冲到进程退出才
flush，落在 pickle 之后，而 `pickle.loads` 忽略尾部多余字节）。所以这不是"必炸"，
是**定时炸弹** —— `print(..., flush=True)` / 输出超过 8KB 缓冲区 / 容器里常见的
`PYTHONUNBUFFERED=1`，都会让它必炸，而报的错是"插件沙箱返回了无法解析的结果
（UnpicklingError）"，stderr 却是空的，用户完全看不出是自己多打了一行调试 print。

| # | 问题 | 修法 |
| --- | --- | --- |
| ① | 插件 print 污染协议 stdout | 插件执行期间 `sys.stdout` 指向 `sys.stderr`，结束后还原 |
| ② | 结果不可 pickle 时先写半截再补错误对象，父进程照样读不了 | 先 `pickle.dump` 到内存缓冲，成功才整体写出 stdout |
| ③ | 多文件插件 import 不了同目录模块 | `sys.path.append` 插件目录（**追加不是插入**，免得插件自带的 `numpy.py` 顶掉标准库） |

新增 **`plugin_worker_test.py`（28 项）**：真实起子进程走 pickle 管道（不 mock，
协议的正确性就在管道上）——正常路径、print / `sys.stdout.write` / 5000 行洪水输出、
插件抛异常 / 没有 `run` / `sys.exit` / 文件不存在、非法请求体与空 stdin 的
`BadRequest`、不可 pickle 结果的 `UnpicklableResult`、多文件插件。

**§6 是反向护栏**：用一段"旧式实现"（不重定向 stdout）实测三种情形 ——
`flush=True` 必炸、小 print 侥幸通过、3000 行必炸，然后断言修复后两种必炸情形
都安全。写测试时别把"侥幸"当成"没问题"，也别为了显得修复重要而夸大缺陷。
测试套件 55 → **56**。

---

## v2.19 — 桌面启动器加固（认得自己人，也认得出代理）

`desktop_launcher.py` 是**唯一一个零测试的产品模块**（105 行）。它管的是用户双击
图标后的第一件事 ——「复用已有实例，还是新起一个」，错了的后果是"开了第二个实例 /
打开了别人的页面 / 报错窗口一闪而过"。补测时摸出三个真缺陷：

| # | 问题 | 实测后果 |
| --- | --- | --- |
| 1 | 回环请求**走代理** | 开了代理工具就永远检测不到已有实例 → 又起一个新实例（端口还不一样，用户以为刚才的数据丢了）。本项目自己的测试都要设 `NO_PROXY`，说明这个坑真实存在 |
| 2 | 身份判定是子串匹配 `'"ok"' in body and "true" in body` | 任何返回 `{"ok": true}` 的第三方服务都被当成"我的另一个实例" —— 而 5000 是 Flask 默认端口，撞车概率不低 |
| 3 | 报错路径 `input()` 无兜底；`app.run` 只捕 `KeyboardInterrupt` | 无控制台（`--windowed` 打包 / CI）时抛 `EOFError`，用户还没看到"无法导入 app"窗口就先炸；端口被抢时甩一屏 traceback |

修法：

- `_http_get` 挂 **空 `ProxyHandler`**，回环请求强制不走代理；
- `is_our_app` 改为 **JSON 解析 + `ok` 为真 + `service` 标记匹配**（老版本没有
  `service` 字段 → **缺字段仍算自家，字段不对才算别人**，既认得老实例又不劫持别人）；
- `_wait_for_enter()` 吞掉 `EOFError`/`OSError`；`app.run` 补 `use_reloader=False`
  与 `OSError` 分支（检测端口和实际绑定之间有时间差，被抢了就给可读提示）。

新增 **`desktop_launcher_test.py`（46 项）**：身份判定 12 例（含"别人的 ok:true"
必须为假、"裸子串 `\"ok\": true`" 必须为假）、**代理回归**（设不可达代理后仍能识别，
并反向断言默认 opener 确实会带环境代理，证明绕过非多余）、端口探测与"全忙抛错"、
`main()` 三条路径（复用 / 新起 / 5000 被占顺延）、导入失败与端口被抢不崩。
真机复核：对**真实 Flask `/health`** `is_our_app` 为 True，设假代理后仍为 True。
测试套件 54 → **55**。

---

## v2.18 — Word 导出内核加固（表格零丢数）

`export_docx.py` 是 v0.9「结果可导出」的核心交付物，214 行，却**只有接口冒烟、
没有一个模块级断言** —— `export_test.py` 走 7 个方法，只验「能打开、有表格」。
于是四个缺陷一直活到今天：

| # | 问题 | 实测后果 |
| --- | --- | --- |
| 1 | `#### 四级标题` 不识别 | 原样输出成 `#### xxx`，在 Word 里变成一行正文 |
| 2 | 整行都是 `-` 的**数据行**被当 GFM 分隔线 | **静默吞掉一行数据** |
| 3 | 数据行比表头多列 | 多出来的格子**静默截断**，数字直接消失 |
| 4 | `markdown=None` | 抛 `AttributeError`（纯函数不该甩栈） |

#2 #3 尤其危险：docx 看起来完全正常，交上去才发现表里少一行／少一列。
对论文工具来说，**静默丢数字比报错糟得多**。

修复要点：

- **标题**：统一用 `^(#{1,6})(\s+|$)` 解析，级数映射 `#`→H1 … `######`→H4。
  `##`→H1、`###`→H2 的历史映射**保留**，不动已有报告的观感。
- **分隔行**：必须**同时**满足三条 —— 紧邻表头（GFM 规定分隔行只在表头后一行）／
  每格只由 `-` `:` 组成／至少一格 ≥3 字符。**宁可多留一行可疑数据，也绝不悄悄删**。
- **列数**：取表头与所有数据行的**最大列数**，短行补空，长行扩列。
- **拆行**：不再用 `strip('|')`（会连剥多个首尾管道符，吃掉行首/行尾的空单元格）。
- **入参宽容**：`markdown` 接受 `None` / `bytes` / 非字符串，`title`、`meta` 同理，
  不再因为传了个 `None` 就炸栈。

新增 **`export_docx_test.py`（97 项）**：标题层级、表格行列完整性、
**数字守恒**（进去的每个数字，出来还在不在）、粗体与列表、图表嵌入（含非法 PNG 降级）、
脏输入 16 例、真实报告端到端（4 个方法 × 8 个数字零丢失）。
测试套件 53 → **54**。

> 顺带记录的工程坑：同一条消息里对**同一个文件**发多个 Edit 会互相覆盖（丢更新）。
> 已实测两次（改 `export_docx.py`、改测试文件各丢一处），必须逐条改、改完复核落盘。

---

## v2.17 — ⑥ 模拟数据生成器收尾（可复现演示数据 + 真值自检）

`simulate.py`（12 个生成器、纯 numpy）此前**只有 CLI 一个入口**：没有
`/api/simulate`、没有前端按钮、**没有一行测试**。而《进一步完善计划》⑥ 的验收
标准恰恰是「生成数据重跑 `run_*` 应得到匹配的统计量」—— 一次都没被验过。

补齐之后，选方法 + 效应量 + 样本量 + 种子，一键生成数据并**直接载入分析流程**
（与真实上传同池，同样触发「上传即体检」）。

| 交付项 | 落地位置 | 状态 |
| --- | --- | --- |
| `GET/POST /api/simulate`（GET 只回方法清单，前端下拉据此生成，不硬编码） | `app.py` | ✅ |
| 前端「🎲 生成演示数据」面板 + 载入分析流程 + truth 展示 + CSV 下载 | `templates/index.html`（`Simulate` 模块） | ✅ |
| 测试 240 项（oracle 交叉验证 / 复现性 / 脏输入 / 端到端） | `simulate_test.py` | ✅ |
| 前端探针 37 项 | `_syntaxcheck/simulate_probe.js` | ✅ |
| CLI `simulate` 非法入参改为一行报错 + 退出码 1（不再甩 traceback） | `cli.py` | ✅ |

### 顺带修掉的四个真缺陷（都是"没测过"才活到今天的）

| # | 问题 | 实测后果 |
| --- | --- | --- |
| 1 | `independent_t` 的 `expected_d` 用 `(g2−g1)/pooled` | 与自己的 `expected_t` 反号（+0.75 vs −0.75），拿它当标准答案会误判工具算错符号 |
| 2 | `two_way_anova` 的交互效应**根本不显著** | 20 个 seed 只中 5 个，中位 p=0.17，而 `note` 写着"有交互效应"——计划验收标准不达标 |
| 3 | `two_way` / `rm_anova` / `logistic` 三个方法**没有任何 `expected_*`** | truth 只有元数据，无法做 oracle 交叉验证 |
| 4 | `cronbach_alpha` 的 `np.clip(alpha, 0, 1)` | α 可以是负数（题目间负相关），实测一份量表 α=−4.86，而"真值"会说 0 —— **美化真值** |

根因（#2 / #4 同源）：`_add_noise` 按 `values.mean()` 缩放噪声，而这两个生成器
的基数都是 50 分量表 —— `noise=0.1` 就加了 sd≈5.5 的噪声，把 effect_size=0.5
的效应整个淹没。现在把噪声加在**离差**上再抬回基数：既保住各单元格同方差
（不破坏 ANOVA 前提），也不再淹掉效应。修完 two_way 交互 **20/20 显著**。

另加一层守门：`generate()` 对 `n<3` / NaN / Inf 明确抛 `ValueError`。
旧版 `n=1` 会在 truth 里产出 **NaN** —— 而 NaN 不在 JSON 标准里，
`jsonify` 会把它序列化成非法 JSON，前端 `JSON.parse` 直接炸。

> **真值的用法**：`expected_*` 可与 `run_*` 精确比对（测试逐位验过 3 个 seed）；
> `true_beta`（logistic）是**生成参数**，估计值只会落在它附近，别当精确 oracle。

---

## v2.16 — ⑨ 协作审阅（核查报告 → 分享链接 + 批注）

把「论文排查」产出的报告变成一个可发给导师、同门的链接：对方打开即可阅读并
追加批注。**内容只落在项目内 `.review_share/`，不依赖任何云服务**，删掉目录即
彻底销毁；`REVIEW_SHARE_DIR=off` 一句话关闭。

| 交付项 | 落地位置 | 状态 |
| --- | --- | --- |
| 分享内核（创建/读取/批注/清理/渲染） | `review_share.py`（纯函数，不 import app） | ✅ |
| `/api/review/share`、`/s/<token>`、`/s/<token>/export`、`/api/review/<token>[/annotate]` | `app.py` | ✅ |
| 前端：核查结果区「🔗 协作审阅」按钮 + 链接卡片 | `templates/index.html`（`ReviewShare` 模块） | ✅ |
| 测试 160 项（含**报告只读**不变量、路径穿越、XSS、过期、端到端） | `review_share_test.py` | ✅ |
| 前端探针 53 项 | `_syntaxcheck/review_share_probe.js` | ✅ |

**四条底线**（改动前请读，别顺手做成"云端分享"）：

1. **报告只读、批注另存** —— 批注只追加/删除，永远不改 `markdown`/`comparisons`。
   报告是证据，意见是意见；物理分开才不会让"协作审阅"变成一条新的造假通道
   （改完再说是导师意见）。这条由测试守着：批注后逐字节比对报告未变。
2. **链接不可猜** —— token 是 96 bit 随机串，**不是自增 ID**（自增等于把所有
   报告按顺序公开）。非法 token 一律 404，**不是 500**（URL 用户可编辑，
   路由层已因此踩过一次）。
3. **无 XSS** —— 分享页**不含任何 JS**，CSP 收到 `default-src 'none'`；
   所有动态内容过 `html.escape`，正文进 `<pre>` 不做 markdown → HTML 转换，
   所以批注里写 `<script>` 也只会显示为文字。
4. **本地优先** —— 不设环境变量就用项目根 `.review_share/`；关闭时路由返回
   503，而不是悄悄往别处写。

`/s/` 前缀对访问门禁放行（分享的初衷就是给没口令的人看），但**批注走页面表单
提交**，不开放成免门禁的写接口；`/api/review/*` 仍在门禁内。

### 附带修复：文本表格的不一致也能被追问、能被分享

v2.12 的**文本形态表格**（Markdown 表 / docx 转出的管道表）只把不一致写进
`suggestions`，而 v2.11 的 **docx 表格**是同时进 `comparisons` 的。同一类问题
两种待遇，后果很具体：

| | docx 表（v2.11） | 文本表（v2.12，修复前） |
| --- | --- | --- |
| 出现在报告正文 | ✅ | ✅ |
| **审计对话里能追问** | ✅ | ❌ 没有 `summary`，点不到 |
| **协作审阅逐条比对里可见** | ✅ | ❌ 分享页按 `comparisons` 渲染 |

现已统一：新增 `audit._table_text_comparisons()` 把文本表不一致转成
`table_mean` / `table_sd` 条目，与 docx 表同口径。顺带修了三处：

1. `_KIND_CN` 补 `table_n` / `table_mean` / `table_sd` —— 否则条目标签
   显示的是英文 key（`table_mean`）而不是中文；
2. 第五节**不再重复**渲染表格条目（5.5d / 5.5e 已有信息更全的专门表，
   重复一行只会让人以为查出了两倍的问题）；只有表格条目时改为指路文案，
   不再误报「没有可对比的统计量」；
3. 表题只在**真像表题**时才带（`表1 …` / `表一 …` / `Table 1` / ≤12 字）。
   `table_check` 取的是表格前一行，那行常常是整句正文，照抄会把结论句撑成
   「论文写 表〈本研究对被试进行了测量，描述统计如下。〉第…行」这种读不通的话。

新增测试 `table_text_bridge_test.py`（42 项），守护的契约是
**报告里看到的条目 ＝ 能追问的条目 ＝ 分享出去的条目**。

### 数据体检报告也能分享（收尾）

协作审阅第一版只接了「论文排查」报告，但**数据体检才是产品入口第一站**——
发导师看"我这份数据有没有问题"比发一份论文排查报告更早、也更有用。现补上：

| 交付项 | 落地位置 | 说明 |
| --- | --- | --- |
| `/api/datacheck` 附带 `markdown` | `app.py` | 复用 `datacheck.render_markdown()`；渲染失败只留空串，**绝不拖垮体检本身** |
| 前端「🔗 协作审阅」按钮 | `templates/index.html` `renderDataCheck` 的 `dc-fixbar` | `ReviewShare.fromDatacheck()` |
| 问题清单 → 分享条目 | 同上 | 体检的 `issues` 映射成 `suggestions`，`kind: 'datacheck'` |

`markdown` 由后端统一渲染，前端不写第四套渲染器——三处体检报告（页面 /
CLI `check-data` / 分享页）保持一种说法。

### 补上分享的管理接口

`list_shares()` / `gc_expired()` 上一轮写完**没有任何调用方**，是死代码。现接上：

| 接口 | 说明 |
| --- | --- |
| `GET /api/review/list?limit=50` | 只返回元信息（标题/时间/批注数/是否过期），**不含报告正文**；`limit` 非数字回落默认值 |
| `POST /api/review/gc` | 只删过期的，返回 `removed` 数量 |

两者都**留在访问门禁内**（与放行的 `/s/<token>` 页面相反）：分享页是给
没口令的人看的，分享列表不是。`review_share_test.py` 因此从 160 项增至 **181 项**。

---

## v2.15 — AI 痕迹自查（BZD 审计框架规则化）

把《BZD 数模论文 AI 痕迹审计》两层框架里纯文本可判定的部分落地为**本地自查**
（`ai_audit.py`，纯规则零 LLM）：高频连接词密度分档（BZD 15/20/25% 阈值）、
句长均匀性（AI 句长标准差小的典型特征）、被动句占比、拔高词无量化数据、
**提示词残留**（"好的，以下是…"等对话式输出直接贴进论文的强信号）、
数模高风险模型红名单（LSTM/遗传算法等，未说明"为什么适合本题"才扣分）、
无边界推广。0-100 风险分 + 五档等级 + P0/P1/P2 修改建议，另附 9 条
机器查不了的人工核对项（摘自《数学建模论文自查表》）。

| 交付项 | 落地位置 | 状态 |
| --- | --- | --- |
| 自查引擎（8 类特征 + 评分合成 + 建议） | `ai_audit.py`（纯函数，不 import app） | ✅ |
| `/api/ai_audit`：传论文文件或粘贴文本即可，无需数据 | `app.py` | ✅ |
| 前端：论文排查 Tab 新增「🧼 AI 痕迹自查」区（风险卡 + 分项扣分 + 建议） | `templates/index.html` | ✅ |
| 测试 23 项（区分度 / 单项 / 红线 / 健壮性 / 端到端） | `ai_audit_test.py` | ✅ |

**红线（与 BZD 指南自身要求一致）**：只评估「AI 风格风险」，**绝不判定是否由
AI 生成**，不替代学校/赛事正式检测；修改建议要求保留事实、数据、公式与引用。
这不是"降 AI 率开关"——定位是把空泛套话改成有据可依的表达。

---

## v2.14 — 可插拔方法市场（plugins/）接入应用

方法以**一个独立 .py 文件**交付：丢进 `plugins/` 重启即可用，不改 app、不改前端、
不动注册表。内置示例插件 **TOST 等价性检验**（纠正"p>0.05 = 没差异"的经典误用）。

| 交付项 | 落地位置 | 状态 |
| --- | --- | --- |
| 插件内核：扫描 / 契约校验 / 沙箱执行（子进程 10s 超时）/ 结果合理性校验 | `plugin_registry.py` + `plugin_worker.py` | ✅ |
| 调度分流：内置注册表不认识的方法自动落插件市场 | `app.py` `_run_plugin_method`（MissingField 后查插件） | ✅ |
| `/api/plugins`：插件清单 + 加载失败原因 + 沙箱模式（exe 内自动降级为进程内并如实标注） | `app.py` + `plugin_registry.plugin_report()` | ✅ |
| 前端：方法下拉动态追加「🧩 插件方法」分组 + 插件参数框 | `templates/index.html` | ✅ |
| 打包：spec 收录 plugin_registry / plugin_worker / plugins/ / static/ 等全部新模块 | `desktop.spec` | ✅ |
| 测试：90+ 项（沙箱超时 / 坏插件容错 / 数值不变量 / 数据不落盘 / 端到端） | `plugin_registry_test.py` | ✅ |

三条护栏：**沙箱执行**（子进程超时即杀，坏插件只废一次调用）、**结果合理性校验**
（p∈[0,1]、df>0、n≥2，不通过绝不进报告）、**契约校验**（单个坏插件不拖垮市场）。

---

## v2.13 — 学术级取证：本福特分布 + 末位偏好

数据体检新增两类**纯 numpy、零 LLM**的学术取证检测器，回答"这批数字像不像人编的 / 人工读的"。
配套的 GRIMMER（查标准差）在本轮收口到 `grimmer.py`，**不再在 `datacheck` 里留第二份**。

| 检验 | 看什么 | 落地位置 |
| --- | --- | --- |
| **本福特分布** | 首位数字应遵循 `log10(1+1/d)`；人随手编数时首位是**均匀**想的 | `datacheck.check_benford` |
| **末位偏好** | 末位数字应均匀（各 10%）；人工读血压常记 120 而非 123 | `datacheck.check_terminal_digits` |
| GRIM（均值） | `n × 均值` 必须是粒度的整数倍 | `datacheck.grim_check` |
| GRIMMER（+SD） | 即便均值可能，SD 仍可能落在整数数据不可达的区间 | **`grimmer.py`（唯一真源）** |

### 为什么用 MAD 而不是卡方（本版最关键的设计取舍）

卡方在样本量大时**几乎必然显著**——任何微小偏离都会被判"不合格"，是一台误报机器。
所以本福特判定改用审计实务（Nigrini）通用的 **MAD（平均绝对偏差）**，它是**与样本量无关**
的刻度；只在 MAD > 0.015（明确不符合）时才报，级别 MID，卡方仅作参考附在证据里。

### 防误报的闸门（每一道都是被真实数据打脸后加的）

- 本福特：列名含编号/年月/量表/评分/百分比/价格 → 跳过；全为正；n ≥ 100；
  跨 ≥ 2 个数量级；唯一值 ≥ 50。
- 末位：**双门槛同时成立**才报（卡方 p < 0.001 **且**某末位占比 ≥ 25%），
  只显著但偏离很小（12% vs 10%）不报。

### 两个曾让检测器形同虚设的 bug（已在测试里锁死护栏）

1. **末位取值**：直接对 `repr(float)` 取末位字符，会把 `120.0` 小数点后的 `0` 当成末位，
   于是任何整数列全部读成 0 —— 既假阳性又让检测器失效。现在先剥掉表示层的补位零。
2. **本福特的量程门**：曾加过"截尾后仍须跨数量级"的分位比闸门，结果线性均匀分布
   （最典型的编造数据）q90/q10 只有约 8，被它误杀，**检测器永不触发**。已移除。
   教训写进代码注释：**宁可留一点点误报率，也不要写一个从不报警的检测器。**

同理，"唯一值数量"曾被当成末位偏好的闸门——但**取整本身就会压低唯一值数量**，
用它当门槛正好把要检测的 heaping 现象过滤掉。现已改为量程 + 唯一值下限 5。

### 其它

- **单一真源**：删掉 `datacheck` 里重复实现的 `grimmer_check`，统一走 `grimmer.py`
  （rsprite2 口径、`Fraction` 精确算术），避免两套 k 空间推导口径分裂。
- 取证类告警**级别最高只到「可疑」**，文案恒含"这只是线索，不代表造假"。
- 测试 `forensics_test.py`（101 项）：真阳性 / 真阴性 / 闸门 / 脏输入 / 文案红线 /
  示例数据零误报。**真阴性额外断言"确实通过了所有前置闸门"**——否则门一开就误报，
  测试却还是绿的。

---

## v2.12 — 文本形态表格核查

`table_check.py`：解析 **Markdown 表 / docx 转文本的管道行**，逐格比 (行, 均值列) / (行, 标准差列)，
与 v2.11 的 docx 结构化表是**双路互补**。容差按论文报告位数动态定（写 `60.90` → 0.005，
写 `60.9` → 0.05）；行标签匹配不到列记 `skipped` 不猜；「一格都没比成」必须明说"未能比对"，
绝不能说"一致"。测试 `table_check_test.py`。详见 `ROADMAP.md`。

---

## v2.11 — 论文表格数字交叉核查

2026-09-14 交付。论文 docx 里的**描述统计表**（组别 | n | M | SD）与原始数据逐格核对，
对不上直接点名到行。只核 docx 结构化表格；分组标签必须唯一命中数据列（对不上/双候选都不猜）；
0.01 舍入差不报；核对不上只说「请核对」。测试 `table_cross_test.py`（29 项）。
详见 `ROADMAP.md`。

---

## v2.10 — 答辩准备包 & 作答时长

2026-09-14 交付。八站流程最后一站（⑧答辩准备）+ 填答质量最后一个检测器（P4 作答时长）：
`defense_pack.build_qa_pack` 五类高频 Q&A（答案引用**后端重算**的统计量，不抄屏幕数字）、
`/api/defense_charts` 会话图表 zip（内存打包不落盘）、`datacheck.check_response_duration`。
测试 `defense_pack_test.py`（30 项）。详见 `ROADMAP.md`。

---

## v2.9 — 多模态图表核查：图配得上结论吗

### 结论先行
规划 §三 ④ 落地。上传一张统计图表 + 论文里对应的一句结论，多模态模型**读图**并判断
「这张图支不支持这句话」。**已用真实 API 跑通验证**，不是只写了代码：

- 输入：一张 matplotlib 柱状图（两组 A/B）+ 结论句「A 组显著高于 B 组」；
- 输出：模型正确读出"横轴组别、纵轴 Score、无误差棒、无显著性标记、未标样本量"；
- **关键行为**：结论说"显著"，但图中没有误差棒 / 星号 / p 值 → 模型返回
  `matches_conclusion: null`（信息不足），**而不是顺从地宣称"显著"**。
  这正是 prompt 里那条硬规则要的效果：「看不出显著性 ≠ 显著」。

### 新增
- **`multimodal_agent.py`**：读图 + 比对结论的纯函数层（`audit_image()`）。
  - **图片不落盘**：以 base64 一次性内联送进请求体，请求结束即释放；
    不写 `uploads/`、不进日志、不进缓存。
  - **缓存键用图片内容的 sha256**（不是文件名 / 路径）：同一张图重复核查直接命中；
    缓存里**只存模型输出的文本**，图片本体永不入库。
  - **宽容解析**：模型输出常带 ```json 围栏或前后解释文字，解析器能抠出
    JSON（含括号平衡器，字符串内的 `{}` 不干扰）；布尔值兼容 `true/符合/是` 等写法；
    `issues` 兼容字符串、字符串数组、对象数组三种形态。
  - **非 JSON 不臆断**：模型没按格式输出时，`matches_conclusion` 置 `null`（绝不当成
    "符合"），并附原文供人工复核。
- **`app.py` → `POST /api/audit_image`**：multipart 入口。
  - 入参校验：空图 / 非法格式 / 超 5 MB → 明确 400/413，不崩溃。
  - **结论句过红线**：防止借"读图"绕过学术不端拦截（与 `audit_chat` 同一道闸门）。
  - **降级不白屏**：缺 Key → HTTP **200** + `fallback:true` + 手动核对提示。
- **`agents/router.py`**：新增 `"audit_image"` 路由状态 + **视觉能力护栏**。
  - 候选链：`glm-4.6v-flash`（智谱免费多模态）→ `qwen3-vl-235b-a22b-thinking`
    → `qwen3-vl-32b-thinking`（自有 MaaS 端点）→ `mimo-v2.5`（BYOK 尾位）。
  - **视觉护栏（本版最重要的防御）**：`VISION_REQUIRED_STATES` + `VISION_MODELS`
    声明"哪些状态必须看图、哪些模型能看图"。容灾链上混入纯文本模型时**直接跳过**——
    因为"图片被静默忽略、模型凭空描述图表"比直接报错**危险得多**：用户会以为模型
    真的看过图了。测试里有一条断言专门锁死"候选链全部具备视觉能力"。
  - `glm-4.6v-flash` 进 `FREE_MODELS`，**免费档（出厂默认）即可用**，无需改档位。
- **`llm_cache.py` → `discard(key)`**：丢弃单条缓存。用于"缓存里的文本解析不出来"
  时清掉坏值，避免后续每次都被同一个坏条目命中、每次都解析失败。
- 测试 **`multimodal_test.py`（55 项）**：路由状态与视觉护栏、6 种模型输出形态解析、
  入参校验、缓存命中与键隔离、铁律②（喂给模型的输入不含原始数据）、端点契约、
  红线拦截、图不落盘、文档一致性。默认 **mock 掉 LLM，不发网络请求**；
  加 `--live` 才真调多模态模型。
- **`docs/API.md`**：补 `/api/audit_image` 完整字段表、降级契约、隐私说明。

### 已知边界（诚实声明）
- **必须真 Key 才能出结果**：这是个 LLM 功能，无 Key 时按设计降级为"请手动核对"。
  这与 CLI / H5 / 小程序那类**纯本地可端到端验证**的功能不同——本轮已用真实
  智谱 Key 跑通主路径，但**线上多模型容灾切换未逐一实测**（只验证了首选档）。
- **模型读图有上限**：极低分辨率、超长图、密集多子图可能读不准；prompt 已要求
  "看不清就明说"，但用户仍需自行复核。

---

## v2.8 — 跨端适配：小程序/Uni-app 接入准备

### 结论先行
**后端零改动即可被微信小程序调用**，这一点已实测确认，不是推测：

- 后端**不使用 cookie 会话**（`set_cookie` 出现 0 次），会话状态全部由服务端
  `_SESSION[file_id]` 承载 → 无 cookie 的小程序天然可用；
- `wx.request / wx.uploadFile` **不走浏览器同源策略**，不看 CORS 头、不发预检
  → 小程序根本用不到 CORS。

所以本轮做的是「补齐 H5/第三方网页需要的跨域能力 + 把小程序接入前提固化成文档与测试」。

### 新增
- **`cross_platform.py`** 跨端适配层（手写 CORS，不引 flask-cors，保持零构建）：
  - **默认全关**：不配 `CORS_ALLOW_ORIGINS` 时不发任何 CORS 头，
    浏览器同源访问行为与 v2.7 完全一致——不为了新端把老端搞坏。
  - 白名单制；必带 `Vary: Origin`（否则 CDN 可能把 A 站响应缓存后发给 B 站）。
  - 预检：允许的来源 `204` + `Max-Age`，**非白名单显式 `403`**
    （而不是静默 200 却没有 CORS 头，后者前端只会看到一个看不懂的报错）。
  - `*` + 凭据的危险组合：回显具体来源而非 `*`（浏览器规范），并在启动时**告警**。
- **`docs/CROSS_PLATFORM.md`**：接入指南。含**阻塞项**（AppID / ICP 备案 + HTTPS /
  微信开发者工具 —— 这三项是外部条件，代码解决不了）、后端配置、小程序调用示例、
  CORS 行为约定表、Uni-app 路线、上线验证清单。
- **`docs/API.md`**：23 个路由的完整 HTTP 参考（由实际路由表整理，含错误码与
  小程序/curl/CLI 三种调用示例）。
- 测试 **`cross_platform_test.py`（72 项）**，锁住：默认零行为改变、白名单判定、
  预检允许/拒绝、通配与凭据的安全处理、小程序接入前提、`file_id` 契约。

### 修复
- **预检被限流器计数（真实缺陷）**：浏览器在每个跨域真实请求前都要先发一次
  `OPTIONS`。预检不消耗业务资源（无计算、无 LLM），却会挤占配额——
  实测连打后从第 60 次起开始返回 429，症状是「跨域调用时好时坏」，极难排查。
  已在 `security_guard` 增加 `is_preflight()` 并让限流钩子豁免 `OPTIONS`。
  **真实请求（GET/POST）照常计数**，安全性不受影响（已用测试双向锁定）。

### 说明
- 本轮**没有**内置 Uni-app 工程：它需要独立工具链与构建产物管理，
  塞进这个「零构建 Python 项目」会破坏开箱即跑的定位。需要时在独立目录建工程，
  直接用本后端的 HTTP 接口即可（接口见 `docs/API.md`）。
- 小程序真正上线仍需补齐三项外部条件；本仓库交付的是
  「可直接接入的后端 + 接入文档 + 契约测试」，而非「已上架的小程序」。

---

## v2.7 — H5 部署：后端零改动，换个服务器外壳

### 新增
- **`wsgi.py` 生产入口**：按运行平台自动挑选 WSGI 服务器——
  Linux/macOS/容器用 **gunicorn**，Windows 用 **waitress**（gunicorn 不支持 Windows）。
  这是「后端一次写、外壳 N 次包」的第一条兑现：**应用代码一行没改**。
- **`gunicorn.conf.py`**：worker 数 / 线程池（`gthread`）/ 日志到 stdout /
  代理信任范围。关键取值都带注释说明业务理由（为什么 `gthread` 而不是 `sync`、
  为什么超时要 >30s）。
- **`Procfile`**：Render / Railway 等 PaaS 自动识别的启动命令。
- **[docs/DEPLOY_H5.md](docs/DEPLOY_H5.md)**：部署指南——生产入口用法、环境变量表、
  Render / Railway / Docker / Nginx 反代四套方案、**上线安全清单**、部署验证步骤、
  常见问题。其中明确写清一个易踩的认知坑：**本项目不能纯静态托管**
  （统计计算在 Python 端，Vercel/Netlify 放不下）。
- 生产依赖 `gunicorn`（非 Windows）/ `waitress`（Windows）按平台条件声明。
- 新增 **`wsgi_test.py`（48 项）**，其中包含一条端到端断言：
  真起生产服务器、真上传、真分析，**与 CLI 的统计量逐字段比对必须全等**。

### 修复
- **Dockerfile 的 `CMD` 用的是开发服务器**：`python app.py` 是 Flask 自带的
  单进程单线程服务器，无优雅重启、无超时管理，对外提供 H5 服务会排队甚至卡死。
  已改为 `gunicorn --config gunicorn.conf.py wsgi:application`。
  （与 v2.5 修掉的 `HOST` 硬编码问题同源：**没人跑过的部署配置等于没写**。）

### 说明
- 本地直启 `python app.py` 保持原样不变 —— 内测就该用开发服务器，方便看错误。
  生产对外服务才需要 `wsgi.py`。
- 新增部署契约测试后，README 里「不能纯静态部署」这条被写进文档并被测试断言，
  避免后来者按「静态托管」的直觉部署后对着一堆 404 困惑。

---

## v2.6 — PWA：把网页变成可安装的桌面应用

### 新增
- **PWA 全套资源**（零成本「伪装桌面端」，Chrome/Edge 地址栏出现「安装」按钮）：
  - `static/manifest.json`：`display: standalone`、品牌色 `#1e40af`、
    192/512 各两份图标（`any` 用于常规显示、`maskable` 用于系统裁切），
    以及「数据分析 / 论文排查 / 论文副驾驶」三个快捷方式（`?tab=` 直达）。
  - `static/sw.js`：**导航请求 network-first**（保证看到最新界面）、
    静态资源 **stale-while-revalidate**、断网时回落到 `offline.html`；
    支持 `SKIP_WAITING` 以便新版本立刻接管。
  - `static/offline.html`：离线页，明确告知「离线状态下不会展示缓存的历史统计结果」。
  - `make_icon.py` 扩展：一次生成 `favicon.ico` + 全套 PWA 图标
    （maskable 版本为满幅底 + 78% 内缩图形，避免被裁成白边）。
  - `app.py` 新增 `/favicon.ico` 路由（带 `Cache-Control: max-age=86400`）；
    `templates/index.html` 接线 manifest / theme-color / apple-touch-icon / SW 注册。

### 安全约束（本版最重要的一条）
- **service worker 显式永不拦截 `/api/*`**（`shouldHandle()` 同时排除非 GET、
  `/api/` 前缀、跨域）。统计工具最危险的故障不是崩溃，而是**用户看到陈旧数字却以为是新结果**。
  该约束由 `pwa_test.py` 的反向断言长期守护。

### 测试基建（本轮最大的隐形收益）
- **可移植性**：清掉 **17 处**硬编码的 `r'D:\论文排版辅助agent\...'` 路径
  （分布在 10 个测试/工具脚本里）。这些路径在 Ubuntu CI 上根本无法 import，
  之前只是"恰好在本机绿"。现统一改为 `pathlib.Path(__file__).resolve().parent` 派生。
- **CI 套件清单不再手工维护**：原清单 29 个，而仓库实际有 **33 个**可跑套件——
  手工清单已悄然漏跑了 `byok / chart / copilot / grim / llm_audit / llm_enhance /
  multi_paper / pwa / stream` 等。改为 `for path in *_test.py` 自动发现 +
  真调脚本黑名单 + 「运行数 < 30 则报错」自检，杜绝清单再次腐烂。
- **`security_guard_test` 自身免疫环境投毒**：脚本内 `os.environ.pop`
  掉外部误设的 `RATE_LIMIT_DISABLE`（此前它在批处理里被全局变量坑成 52/4；
  单跑是 56/0。该套件测的就是限流器本身）。CI 的 `env:` 也删掉了这个全局变量。
- **真调脚本不再污染回归**：`kimi_mimo_live_test.py` 加 `--live` 显式门槛，
  不带则打印 SKIP 并 `exit 0`。此前它被 `*_test.py` 批扫到，必然 SIGTERM 超时，
  还会因上游额度/限流产生与代码无关的失败。
  同时修掉它文档里引用的不存在的 `kimi_mimo_test.py`。
- 新增 `pwa_test.py`（57 项）。

### 修复
- 确认并清理了端口 5000 上的**残留旧进程**：Windows 允许多进程绑定同一端口，
  操作系统会在它们之间负载均衡，表现为"同一 URL 时好时坏"（此前 `/favicon.ico`
  的间歇 404 即由此而来）。已终止孤儿进程并验证单进程下全部 PWA 端点 200。

---

## v2.5 — CLI 跨端与可复现性修复

### 新增
- **`cli.py` 命令行外壳**：`analyze` / `audit` / `simulate` / `check-data` / `methods`
  五个子命令，走与网页端完全相同的纯函数链路，结果逐字节一致。CLI 默认不写文件、
  只打印（`--output` 才落盘），红线自检同样生效。配套 `cli_test.py`（69 项）。
  `check-data` 可作 CI 门控（发现高优先级数据问题返回退出码 1，`--exit-zero` 可关闭）。

### 修复
- **`cli.py audit` 此前从未可用**：引用了不存在的 `audit_paper`。
  现接回 `build_audit_report`，并复用 `extract_paper.read_paper_text`
  （`.docx / .pdf / .txt / .md` 全支持）、`app._summarize_column`。
- **报告不可复现（重要）**：`extract_quantities` 用 `str.__hash__()` 当排序键，
  而字符串 hash 受 `PYTHONHASHSEED` 随机化影响 → 同一篇论文每次运行报告行序都不同。
  改为按匹配位置排序。对「统计可复现核查」工具而言，这是可信度问题。
- CLI 红线拦截路径不再外漏 `AttributeError` 堆栈，并输出正确用法指引。
- 字段缺失时给出「需要哪些字段 / 当前有哪些列 / 参数怎么对应」而非一句原始错误。

### 测试维护
- 修掉三处**快照式过期断言**（`compat_agent_test` / `router_cooldown_test`）：
  v1.8 新增 `kimi` / `mimo` 平台后，`== {5 个平台}` 之类断言误报。
  改为契约断言（核心平台存在、免费档以 MaaS 收尾）。

---

## v2.1 — 数据体检全链路

### 新增
- **`datacheck.py`**（产品入口第一站）：9 个检测器 + `grim_check()`，
  纯本地规则、零 LLM、零新依赖。检出：合计≠分项 / 取值越界 / 计数列小数 /
  重复行 / 前后测差值规律 / 常数列 / 反向题漏反向计分 / 直线作答 / 缺失模式。
- **一键清洗副本**（第二站）：`propose_fix(df, report)` + `POST /api/datacheck/fix`。
  只自动修「语义无歧义」的两类（重复行、反向题反向计分），**绝不修改原文件**。
  去重延后到最后统一执行，保证 `actions[].rows` 始终是原始 Excel 行号。
- 前端 Tab1 **上传即体检**（可折叠报告卡片 + 清洗按钮）。

### 修复（体检过程中暴露的真实缺陷）
- GRIM 公式漏乘 `n`（应校验 `n × mean` 是否为题项数的整数倍，而非看小数位）。
- 小样本越界漏报：原用「多数一致≥90%」判据，6 行数据里 1 个越界值报不出来。
  改为「中位数落在合理区间 → 任何越界值都可疑」，对 n 不敏感。
- 窄表假重复：粗粒度去重把 `two_way_data.csv` 误判出 46 行「重复」。
  改用可解释判据（必须存在「近似唯一列」）后，5 份内置示例 0 误报。

### 测试
- `datacheck_test.py`（77 项）、`datacheck_ui_test.py`（25 项）。

---

## v2.0 — 产品主线重构

### 新增
- **《专属桌面端开发总纲.md》**：把「数据体检」提升为整个产品的入口，
  确立八站流程图（上传 → 数据体检 → 清洗 → 方法推荐 → 统计计算 →
  人话解读 → 论文核查 → 写作/Word → 答辩准备）。
- 关键洞察：③–⑦ 站早已实装，①② 是新增入口 —— **换一个入口就换了一个产品**。

---

## v1.8 — 平台扩展

- 新增 **Kimi（月之暗面）** 与 **小米 MiMo** 两个平台（付费档 BYOK 尾部备胎，
  故意排在免费候选之后，免费用户零感知；`set_user_key()` 过的平台自动提到链首）。
- 平台总数达 7 家：`sf / zhipu / deepseek / dashscope / maas / kimi / mimo`。

---

## v1.7 — 人味增强

- **`tone_guide.py`** 语气规约：7 条可判定禁令（拆长句 / 删空泛总结 / 不堆排比 /
  结论句必须带统计量与样本量 / 少用套话 / 术语一致 / 不写小标题式废话），
  外加「建议必须可执行」审稿版。
- `AI_SPEAK_PATTERNS` + `find_ai_speak()`：规则化体检，**只报不改**。
- 注入两条冻结 prompt，并按契约 bump `PROMPT_VERSION` 到 v3。
- 定位到的真问题：原 prompt 里「避免 AI 腔」是句没有可判定标准的空话；
  而其余 8 个内置报告模板经检测**本身零 AI 腔**，无需改写。

---

## v1.6 — 阅读导航与审计对话

- **结果页阅读导航**（`ReadingNav`）：三个 Tab 的结果区统一支持目录跳读；
  `scan()` 同时识别 markdown 标题与面板自绘的 `[data-sec]` 小标题。
  护栏：标题少于 3 个不出目录，短报告零打扰。
- **②审计对话**：对审计中的**一条比对**追问。LLM 只解释、绝不计算；
  输入用白名单构造（不用 `**summary` 展开），无 Key 时回退规则引擎的 `verdict`
  模板，永远有话说、不白屏。提问本身也过红线检查。

---

## v1.5 — 学术红线自检

- **红线引擎**：`core` 类（代写 / 买卖 / 规避查重 / 伪造数据）无条件拦截；
  `writing` 类在有明确润色意图时可豁免。后端在 `/api/check_paper` 拦指令，
  前端在 `/api/red_line_scan` 提示。
- **③人话解释卡片（XAI）**：结构化 `explanations` 与旧 `suggestions: list[str]`
  并行输出，`fix` 字段必须指向真实存在的方法 key。

---

## v1.2 — 统计方法补齐

- **重复测量方差分析**：被试内设计、Mauchly 球形度检验 + Greenhouse-Geisser 校正、
  Bonferroni 事后比较。
- **论文副驾驶六阶段流水线**：资料调研 → 文献综述 → 研究设计 → 数据分析 →
  论文核查 → 论文撰写；阶段产出契约 + 顺序门控 + 断点恢复。
- **证据约束写作引擎**：claim 台账（来源 / 基线 / 置信度）+ 写作闸门自检
  （无来源不进正文、非 high 不进摘要、大词必须有数字）。
- LaTeX 与中文学位论文双模板。

---

## v1.1 — 量表与信度

- **Cronbach's α 信度分析**（α、删项后 α、CITC、信度等级建议）。
- **双因素方差分析**（Type III 平方和、主效应 + 交互效应）。
- **分步引导向导**，Likert 1–5 量表列识别。
- 固化「新增统计方法需同步修改 N 处」的约定。

---

## v1.0 — 回归与首个公开版本

- **多元线性回归**（OLS、R²、调整 R²、F 检验、系数 t 检验 + 95% CI、VIF）。
- **二元 Logistic 回归**（IRLS、Wald z、OR 值、McFadden 伪 R²）。
- 首个公开版本。

---

## v0.9 — 导出与文本提取

- **导出 Word**：标题 / 表格 / 列表自动排版 + 图表嵌入 + 封面元信息。
- `.pdf` 论文文本提取（含知网/万方 AES 加密 PDF 的空密码解密）。

---

## v0.8 — 流式渲染

- SSE 事件流（阶段消息 + 进度条 + 先文后图），`stream=0` 保留非流式兼容。

---

## v0.7 — 非参数方法

- **配对样本 T 检验**、**Mann-Whitney U**、**Wilcoxon 符号秩检验**（含效应量）。

---

## v0.5 — 多平台 LLM 路由

- 多平台容灾链 + 结果缓存 + 前缀缓存 + 失败静默降级。
- 免安装桌面版（PyInstaller）。
- 论文排查支持自然语言指令过滤（「只看 T 检验 / p<0.05 / 男组」）。

---

## v0.4 及更早

- **独立样本 T 检验**、**单因素 ANOVA**、**Pearson 相关**、**卡方检验**，
  以及列类型自动识别与推荐方法。
