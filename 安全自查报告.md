# 智论助手 · 安全自查报告

> 扫描日期：2026-09-17 ｜ 范围：全仓（Python 后端 / 前端 / 部署配置 / Git 历史）
> 结论先行：**能防住"被狂刷额度"，前提是上线时按本文 §4 把三个环境变量配上。**
> 扫描中发现 1 个真实漏洞（BYOK 用户 Key 跨用户泄露），**已在本次修复**（v2.25）。

---

## 1. 你关心的三个问题，直接回答

### Q1：会不会被别人狂刷额度、把成本刷爆？

**不会烧钱，但可能被刷"拒"。** 机制如下：

| 防线 | 现状 | 说明 |
| --- | --- | --- |
| 成本封顶（架构级） | ✅ 出厂默认 `tier=free` | 服务端只走**零成本模型**白名单（智谱 glm-4.7-flash、百炼 qwen3.7-flash、GLM-Z1-9B、自有 MaaS 端点）。付费模型必须显式 `LLM_TIER=pro` 才解锁——你不设，谁也碰不到付费模型 |
| LLM 接口单独限流 | ✅ 每 IP 每分钟 8 次 | `security_guard.py`，滑动窗口，超限回 429 + Retry-After。刷的人最多把免费档的平台侧 RPM 打满（表现为其他用户暂时降级为规则模板），**你的钱一分不少** |
| 普通接口限流 | ✅ 每 IP 每分钟 60 次 | 同上，内存有界（最多跟踪 4096 个 IP，空闲 5 分钟回收） |
| 上传体积封顶 | ✅ 单文件 20 MB | `MAX_CONTENT_LENGTH`，防大文件撑爆内存 |
| BYOK 用户 | ✅ 花的是用户自己的 Key | 六家平台用户自带 Key，与你无关 |

**残留风险（可接受）**：限流是**按 IP** 的。校园网/公司 NAT 下多人共用一个出口 IP 会共享额度（误伤好人）；攻击者换代理池可绕过单 IP 限制（但打满的也是平台免费额度，不是你的钱）。要更严可以把 `RATE_LIMIT_LLM_PER_MIN` 调到 4。

### Q2：会不会被"破解"？

- **没有可破解的账号体系**：没有注册、没有用户库、没有 JWT 密钥可偷。门禁只有一个共享口令（`ACCESS_CODE`），cookie 里存的是 **HMAC-SHA256 摘要**（常量时间比较，支持中文口令），拿到 cookie 也反推不出口令；改口令全员旧 cookie 立即失效。
- **没有硬编码密钥**：全仓扫描无任何 `sk-...` 形态 Key；`.env` 被 `.gitignore` 排除且 **Git 历史中从未入库**（已验证 `git log -- .env` 为空）。
- **错误不外泄**：500 统一回通用文案、不回堆栈；`secrets_guard` 对所有出口文案做 Key 擦除（精确替换 + `sk-` 形态兜底）。
- **上传解析在内存完成**，不落盘，符合"用户数据全内存"红线。

### Q3：代码全在 GitHub 公开，安全吗？

公开仓库（`zhilun-assistant`）里**没有任何秘密**：口令、Key 全走环境变量，样例文件 `.env.example` 是空模板。内部文档（参赛方案、营销稿、小说稿）和第三方语料（模板论文/、freellmapi/）都在 `.gitignore` 里，确认未入库。**开源反而让安全设计可被审查，不降低安全性**——前提是永远不要图省事把 Key 写进代码（CI 无此检查，靠自律）。

---

## 2. 本次发现并修复的漏洞（v2.25）

### 🔴 BYOK 用户 Key 跨用户泄露（已修复）

**问题**：`Router` 是进程级单例，用户 A 在页面上填的自带 Key 被写进单例的普通 dict，请求结束后**永久残留**。后果：

1. 用户 B 之后发起的请求（自己没填 Key）会被路由到 A 的 Key 上——**盗用 A 的额度**，A 若是付费 Key 就是真金白银；
2. 带 A 的 Key 的 Agent 实例还会进共享缓存，加速扩散；
3. 并发请求时两个用户的 Key 互相覆盖（竞态）。

**修复**（`agents/router.py`）：

- `_user_keys` 改为 **contextvars 请求级隔离**：每个请求线程各拿一份，请求结束线程销毁，Key 零残留；
- 带用户 Key 的 Agent **不进共享缓存、不污染 `_resolved`**，用完即弃；
- `_resolved` 复用捷径加防护：当前请求带了某平台用户 Key 时，必须绕开缓存里的环境变量 Agent；
- 前端 `/api/audit_chat`、`/api/audit_image` 补上 `collectByok()` 透传（这两个入口原先依赖服务端残留才"碰巧能用"，隔离后必须每次自带）；
- 新增 7 条隔离断言（`byok_test.py` §9，含多线程模拟另一用户）+ 前端透传点契约探针（`_syntaxcheck/byok_fields_probe.js`）。

**验证**：byok 49/0、kimi_mimo 41/0、router_cooldown 46/0、security_guard 46/0、secrets_guard 49/0，全量回归绿（见 `_tmp_reg.txt`）。

---

## 3. 上线前必做（三件事，缺一门禁等于没装）

```bash
ACCESS_CODE=你的口令          # 1. 开门禁：没口令连页面都打不开
RATE_LIMIT_LLM_PER_MIN=8      # 2. LLM 限流（默认已开，可按需调紧到 4）
ACCESS_COOKIE_SECURE=1        # 3. 配好 HTTPS 后再开（HTTP 下开了会登录死循环）
```

- 反代（Nginx/Cloudflare）后面才设 `TRUST_PROXY=1`，**直连公网千万别设**（否则任何人伪造 `X-Forwarded-For` 换 IP 绕过限流）。
- 生产环境绝不设 `RATE_LIMIT_DISABLE=1` / `DEBUG=1`。

## 4. 已知局限（如实告知，不粉饰）

> **更新（2026-09-20，v2.27）**：下表第 1、2 条已在本轮修复——
> `/unlock`（连同 /login、/register）获得**独立防爆破锁定**（`security_guard.LoginLockout`：
> 5 次失败锁 15 分钟，锁定期内正确口令同样被拒）；「限额按 IP 不按人」由
> **独立账号系统**解决（`ACCOUNTS_ENABLED=1`：每人注册登录，限流按用户名计数、
> `file_id` 归属本人）。第 3、4 条维持原状（设计如此 / 部署层缓解）。

| 项 | 说明 | 建议 |
| --- | --- | --- |
| ~~口令爆破~~（✅ v2.27 已修） | ~~`/unlock` 只吃 60 次/分钟 的通用限流，无独立锁定~~ → 独立锁定已上线 | 口令仍建议 8 位以上混合 |
| ~~无按人限额~~（✅ v2.27 已修） | ~~限额按 IP 不按人~~ → 账号系统上线后按用户名计数、数据按人隔离 | 开 `ACCOUNTS_ENABLED=1` 即生效 |
| 分享链接 `/s/<token>` | 96bit 不可猜，但**在门禁外**（设计如此：给没口令的导师看） | 别把含敏感数据的报告随手分享 |
| 门禁是"看门人" | 防"网址被转发给陌生人"，不防定向攻击 | 真被盯上时上 Cloudflare（免费 WAF + DDoS 防护） |

---

*扫描方式：全仓源码阅读（security_guard / access_guard / router / app / secrets_guard / 前端 / .gitignore / Git 历史），未发现其他高危项。*
