# 更新日志

本项目遵循「版本号体现在功能里程碑」的惯例。当前版本 **v2.14**。

---

## v2.14 — 可插拔方法市场（plugins/）接入应用

方法以**一个独立 .py 文件**交付：丢进 `plugins/` 重启即可用，不改 app、不改前端、
不动注册表。内置示例插件 **TOST 等价性检验**（纠正"p>0.05 = 没差异"的经典误用）。

| 交付项 | 落地位置 | 状态 |
| --- | --- | --- |
| 插件内核：扫描 / 契约校验 / 沙箱执行（子进程 10s 超时）/ 结果合理性校验 | `plugin_registry.py` + `plugin_worker.py` | ✅ |
| 调度分流：内置注册表不认识的方法自动落插件市场 | `app.py` `_run_plugin_method`（MissingField 后查插件） | ✅ |
| `/api/plugins`：插件清单 + 加载失败原因 + 沙箱模式（exe 内自动降级为进程内并如实标注） | `app.py` + `plugin_registry.plugin_report()` | ✅ |
| 前端：方法下拉动态追加「🧩 插件方法」分组 + 插件参数框 | `templates/index.html` | ✅ |
| 打包：spec 收录 plugin_registry / plugin_worker / plugins/ / static/ 等全部新模块 | `desktop.spec` | ✅ |
| 测试：90+ 项（沙箱超时 / 坏插件容错 / 数值不变量 / 数据不落盘 / 端到端） | `plugin_registry_test.py` | ✅ |

三条护栏：**沙箱执行**（子进程超时即杀，坏插件只废一次调用）、**结果合理性校验**
（p∈[0,1]、df>0、n≥2，不通过绝不进报告）、**契约校验**（单个坏插件不拖垮市场）。

---

## v2.13 — 学术级取证：本福特分布 + 末位偏好

数据体检新增两类**纯 numpy、零 LLM**的学术取证检测器，回答"这批数字像不像人编的 / 人工读的"。
配套的 GRIMMER（查标准差）在本轮收口到 `grimmer.py`，**不再在 `datacheck` 里留第二份**。

| 检验 | 看什么 | 落地位置 |
| --- | --- | --- |
| **本福特分布** | 首位数字应遵循 `log10(1+1/d)`；人随手编数时首位是**均匀**想的 | `datacheck.check_benford` |
| **末位偏好** | 末位数字应均匀（各 10%）；人工读血压常记 120 而非 123 | `datacheck.check_terminal_digits` |
| GRIM（均值） | `n × 均值` 必须是粒度的整数倍 | `datacheck.grim_check` |
| GRIMMER（+SD） | 即便均值可能，SD 仍可能落在整数数据不可达的区间 | **`grimmer.py`（唯一真源）** |

### 为什么用 MAD 而不是卡方（本版最关键的设计取舍）

卡方在样本量大时**几乎必然显著**——任何微小偏离都会被判"不合格"，是一台误报机器。
所以本福特判定改用审计实务（Nigrini）通用的 **MAD（平均绝对偏差）**，它是**与样本量无关**
的刻度；只在 MAD > 0.015（明确不符合）时才报，级别 MID，卡方仅作参考附在证据里。

### 防误报的闸门（每一道都是被真实数据打脸后加的）

- 本福特：列名含编号/年月/量表/评分/百分比/价格 → 跳过；全为正；n ≥ 100；
  跨 ≥ 2 个数量级；唯一值 ≥ 50。
- 末位：**双门槛同时成立**才报（卡方 p < 0.001 **且**某末位占比 ≥ 25%），
  只显著但偏离很小（12% vs 10%）不报。

### 两个曾让检测器形同虚设的 bug（已在测试里锁死护栏）

1. **末位取值**：直接对 `repr(float)` 取末位字符，会把 `120.0` 小数点后的 `0` 当成末位，
   于是任何整数列全部读成 0 —— 既假阳性又让检测器失效。现在先剥掉表示层的补位零。
2. **本福特的量程门**：曾加过"截尾后仍须跨数量级"的分位比闸门，结果线性均匀分布
   （最典型的编造数据）q90/q10 只有约 8，被它误杀，**检测器永不触发**。已移除。
   教训写进代码注释：**宁可留一点点误报率，也不要写一个从不报警的检测器。**

同理，"唯一值数量"曾被当成末位偏好的闸门——但**取整本身就会压低唯一值数量**，
用它当门槛正好把要检测的 heaping 现象过滤掉。现已改为量程 + 唯一值下限 5。

### 其它

- **单一真源**：删掉 `datacheck` 里重复实现的 `grimmer_check`，统一走 `grimmer.py`
  （rsprite2 口径、`Fraction` 精确算术），避免两套 k 空间推导口径分裂。
- 取证类告警**级别最高只到「可疑」**，文案恒含"这只是线索，不代表造假"。
- 测试 `forensics_test.py`（101 项）：真阳性 / 真阴性 / 闸门 / 脏输入 / 文案红线 /
  示例数据零误报。**真阴性额外断言"确实通过了所有前置闸门"**——否则门一开就误报，
  测试却还是绿的。

---

## v2.12 — 文本形态表格核查

`table_check.py`：解析 **Markdown 表 / docx 转文本的管道行**，逐格比 (行, 均值列) / (行, 标准差列)，
与 v2.11 的 docx 结构化表是**双路互补**。容差按论文报告位数动态定（写 `60.90` → 0.005，
写 `60.9` → 0.05）；行标签匹配不到列记 `skipped` 不猜；「一格都没比成」必须明说"未能比对"，
绝不能说"一致"。测试 `table_check_test.py`。详见 `ROADMAP.md`。

---

## v2.11 — 论文表格数字交叉核查

2026-09-14 交付。论文 docx 里的**描述统计表**（组别 | n | M | SD）与原始数据逐格核对，
对不上直接点名到行。只核 docx 结构化表格；分组标签必须唯一命中数据列（对不上/双候选都不猜）；
0.01 舍入差不报；核对不上只说「请核对」。测试 `table_cross_test.py`（29 项）。
详见 `ROADMAP.md`。

---

## v2.10 — 答辩准备包 & 作答时长

2026-09-14 交付。八站流程最后一站（⑧答辩准备）+ 填答质量最后一个检测器（P4 作答时长）：
`defense_pack.build_qa_pack` 五类高频 Q&A（答案引用**后端重算**的统计量，不抄屏幕数字）、
`/api/defense_charts` 会话图表 zip（内存打包不落盘）、`datacheck.check_response_duration`。
测试 `defense_pack_test.py`（30 项）。详见 `ROADMAP.md`。

---

## v2.9 — 多模态图表核查：图配得上结论吗

### 结论先行
规划 §三 ④ 落地。上传一张统计图表 + 论文里对应的一句结论，多模态模型**读图**并判断
「这张图支不支持这句话」。**已用真实 API 跑通验证**，不是只写了代码：

- 输入：一张 matplotlib 柱状图（两组 A/B）+ 结论句「A 组显著高于 B 组」；
- 输出：模型正确读出"横轴组别、纵轴 Score、无误差棒、无显著性标记、未标样本量"；
- **关键行为**：结论说"显著"，但图中没有误差棒 / 星号 / p 值 → 模型返回
  `matches_conclusion: null`（信息不足），**而不是顺从地宣称"显著"**。
  这正是 prompt 里那条硬规则要的效果：「看不出显著性 ≠ 显著」。

### 新增
- **`multimodal_agent.py`**：读图 + 比对结论的纯函数层（`audit_image()`）。
  - **图片不落盘**：以 base64 一次性内联送进请求体，请求结束即释放；
    不写 `uploads/`、不进日志、不进缓存。
  - **缓存键用图片内容的 sha256**（不是文件名 / 路径）：同一张图重复核查直接命中；
    缓存里**只存模型输出的文本**，图片本体永不入库。
  - **宽容解析**：模型输出常带 ```json 围栏或前后解释文字，解析器能抠出
    JSON（含括号平衡器，字符串内的 `{}` 不干扰）；布尔值兼容 `true/符合/是` 等写法；
    `issues` 兼容字符串、字符串数组、对象数组三种形态。
  - **非 JSON 不臆断**：模型没按格式输出时，`matches_conclusion` 置 `null`（绝不当成
    "符合"），并附原文供人工复核。
- **`app.py` → `POST /api/audit_image`**：multipart 入口。
  - 入参校验：空图 / 非法格式 / 超 5 MB → 明确 400/413，不崩溃。
  - **结论句过红线**：防止借"读图"绕过学术不端拦截（与 `audit_chat` 同一道闸门）。
  - **降级不白屏**：缺 Key → HTTP **200** + `fallback:true` + 手动核对提示。
- **`agents/router.py`**：新增 `"audit_image"` 路由状态 + **视觉能力护栏**。
  - 候选链：`glm-4.6v-flash`（智谱免费多模态）→ `qwen3-vl-235b-a22b-thinking`
    → `qwen3-vl-32b-thinking`（自有 MaaS 端点）→ `mimo-v2.5`（BYOK 尾位）。
  - **视觉护栏（本版最重要的防御）**：`VISION_REQUIRED_STATES` + `VISION_MODELS`
    声明"哪些状态必须看图、哪些模型能看图"。容灾链上混入纯文本模型时**直接跳过**——
    因为"图片被静默忽略、模型凭空描述图表"比直接报错**危险得多**：用户会以为模型
    真的看过图了。测试里有一条断言专门锁死"候选链全部具备视觉能力"。
  - `glm-4.6v-flash` 进 `FREE_MODELS`，**免费档（出厂默认）即可用**，无需改档位。
- **`llm_cache.py` → `discard(key)`**：丢弃单条缓存。用于"缓存里的文本解析不出来"
  时清掉坏值，避免后续每次都被同一个坏条目命中、每次都解析失败。
- 测试 **`multimodal_test.py`（55 项）**：路由状态与视觉护栏、6 种模型输出形态解析、
  入参校验、缓存命中与键隔离、铁律②（喂给模型的输入不含原始数据）、端点契约、
  红线拦截、图不落盘、文档一致性。默认 **mock 掉 LLM，不发网络请求**；
  加 `--live` 才真调多模态模型。
- **`docs/API.md`**：补 `/api/audit_image` 完整字段表、降级契约、隐私说明。

### 已知边界（诚实声明）
- **必须真 Key 才能出结果**：这是个 LLM 功能，无 Key 时按设计降级为"请手动核对"。
  这与 CLI / H5 / 小程序那类**纯本地可端到端验证**的功能不同——本轮已用真实
  智谱 Key 跑通主路径，但**线上多模型容灾切换未逐一实测**（只验证了首选档）。
- **模型读图有上限**：极低分辨率、超长图、密集多子图可能读不准；prompt 已要求
  "看不清就明说"，但用户仍需自行复核。

---

## v2.8 — 跨端适配：小程序/Uni-app 接入准备

### 结论先行
**后端零改动即可被微信小程序调用**，这一点已实测确认，不是推测：

- 后端**不使用 cookie 会话**（`set_cookie` 出现 0 次），会话状态全部由服务端
  `_SESSION[file_id]` 承载 → 无 cookie 的小程序天然可用；
- `wx.request / wx.uploadFile` **不走浏览器同源策略**，不看 CORS 头、不发预检
  → 小程序根本用不到 CORS。

所以本轮做的是「补齐 H5/第三方网页需要的跨域能力 + 把小程序接入前提固化成文档与测试」。

### 新增
- **`cross_platform.py`** 跨端适配层（手写 CORS，不引 flask-cors，保持零构建）：
  - **默认全关**：不配 `CORS_ALLOW_ORIGINS` 时不发任何 CORS 头，
    浏览器同源访问行为与 v2.7 完全一致——不为了新端把老端搞坏。
  - 白名单制；必带 `Vary: Origin`（否则 CDN 可能把 A 站响应缓存后发给 B 站）。
  - 预检：允许的来源 `204` + `Max-Age`，**非白名单显式 `403`**
    （而不是静默 200 却没有 CORS 头，后者前端只会看到一个看不懂的报错）。
  - `*` + 凭据的危险组合：回显具体来源而非 `*`（浏览器规范），并在启动时**告警**。
- **`docs/CROSS_PLATFORM.md`**：接入指南。含**阻塞项**（AppID / ICP 备案 + HTTPS /
  微信开发者工具 —— 这三项是外部条件，代码解决不了）、后端配置、小程序调用示例、
  CORS 行为约定表、Uni-app 路线、上线验证清单。
- **`docs/API.md`**：23 个路由的完整 HTTP 参考（由实际路由表整理，含错误码与
  小程序/curl/CLI 三种调用示例）。
- 测试 **`cross_platform_test.py`（72 项）**，锁住：默认零行为改变、白名单判定、
  预检允许/拒绝、通配与凭据的安全处理、小程序接入前提、`file_id` 契约。

### 修复
- **预检被限流器计数（真实缺陷）**：浏览器在每个跨域真实请求前都要先发一次
  `OPTIONS`。预检不消耗业务资源（无计算、无 LLM），却会挤占配额——
  实测连打后从第 60 次起开始返回 429，症状是「跨域调用时好时坏」，极难排查。
  已在 `security_guard` 增加 `is_preflight()` 并让限流钩子豁免 `OPTIONS`。
  **真实请求（GET/POST）照常计数**，安全性不受影响（已用测试双向锁定）。

### 说明
- 本轮**没有**内置 Uni-app 工程：它需要独立工具链与构建产物管理，
  塞进这个「零构建 Python 项目」会破坏开箱即跑的定位。需要时在独立目录建工程，
  直接用本后端的 HTTP 接口即可（接口见 `docs/API.md`）。
- 小程序真正上线仍需补齐三项外部条件；本仓库交付的是
  「可直接接入的后端 + 接入文档 + 契约测试」，而非「已上架的小程序」。

---

## v2.7 — H5 部署：后端零改动，换个服务器外壳

### 新增
- **`wsgi.py` 生产入口**：按运行平台自动挑选 WSGI 服务器——
  Linux/macOS/容器用 **gunicorn**，Windows 用 **waitress**（gunicorn 不支持 Windows）。
  这是「后端一次写、外壳 N 次包」的第一条兑现：**应用代码一行没改**。
- **`gunicorn.conf.py`**：worker 数 / 线程池（`gthread`）/ 日志到 stdout /
  代理信任范围。关键取值都带注释说明业务理由（为什么 `gthread` 而不是 `sync`、
  为什么超时要 >30s）。
- **`Procfile`**：Render / Railway 等 PaaS 自动识别的启动命令。
- **[docs/DEPLOY_H5.md](docs/DEPLOY_H5.md)**：部署指南——生产入口用法、环境变量表、
  Render / Railway / Docker / Nginx 反代四套方案、**上线安全清单**、部署验证步骤、
  常见问题。其中明确写清一个易踩的认知坑：**本项目不能纯静态托管**
  （统计计算在 Python 端，Vercel/Netlify 放不下）。
- 生产依赖 `gunicorn`（非 Windows）/ `waitress`（Windows）按平台条件声明。
- 新增 **`wsgi_test.py`（48 项）**，其中包含一条端到端断言：
  真起生产服务器、真上传、真分析，**与 CLI 的统计量逐字段比对必须全等**。

### 修复
- **Dockerfile 的 `CMD` 用的是开发服务器**：`python app.py` 是 Flask 自带的
  单进程单线程服务器，无优雅重启、无超时管理，对外提供 H5 服务会排队甚至卡死。
  已改为 `gunicorn --config gunicorn.conf.py wsgi:application`。
  （与 v2.5 修掉的 `HOST` 硬编码问题同源：**没人跑过的部署配置等于没写**。）

### 说明
- 本地直启 `python app.py` 保持原样不变 —— 内测就该用开发服务器，方便看错误。
  生产对外服务才需要 `wsgi.py`。
- 新增部署契约测试后，README 里「不能纯静态部署」这条被写进文档并被测试断言，
  避免后来者按「静态托管」的直觉部署后对着一堆 404 困惑。

---

## v2.6 — PWA：把网页变成可安装的桌面应用

### 新增
- **PWA 全套资源**（零成本「伪装桌面端」，Chrome/Edge 地址栏出现「安装」按钮）：
  - `static/manifest.json`：`display: standalone`、品牌色 `#1e40af`、
    192/512 各两份图标（`any` 用于常规显示、`maskable` 用于系统裁切），
    以及「数据分析 / 论文排查 / 论文副驾驶」三个快捷方式（`?tab=` 直达）。
  - `static/sw.js`：**导航请求 network-first**（保证看到最新界面）、
    静态资源 **stale-while-revalidate**、断网时回落到 `offline.html`；
    支持 `SKIP_WAITING` 以便新版本立刻接管。
  - `static/offline.html`：离线页，明确告知「离线状态下不会展示缓存的历史统计结果」。
  - `make_icon.py` 扩展：一次生成 `favicon.ico` + 全套 PWA 图标
    （maskable 版本为满幅底 + 78% 内缩图形，避免被裁成白边）。
  - `app.py` 新增 `/favicon.ico` 路由（带 `Cache-Control: max-age=86400`）；
    `templates/index.html` 接线 manifest / theme-color / apple-touch-icon / SW 注册。

### 安全约束（本版最重要的一条）
- **service worker 显式永不拦截 `/api/*`**（`shouldHandle()` 同时排除非 GET、
  `/api/` 前缀、跨域）。统计工具最危险的故障不是崩溃，而是**用户看到陈旧数字却以为是新结果**。
  该约束由 `pwa_test.py` 的反向断言长期守护。

### 测试基建（本轮最大的隐形收益）
- **可移植性**：清掉 **17 处**硬编码的 `r'D:\论文排版辅助agent\...'` 路径
  （分布在 10 个测试/工具脚本里）。这些路径在 Ubuntu CI 上根本无法 import，
  之前只是"恰好在本机绿"。现统一改为 `pathlib.Path(__file__).resolve().parent` 派生。
- **CI 套件清单不再手工维护**：原清单 29 个，而仓库实际有 **33 个**可跑套件——
  手工清单已悄然漏跑了 `byok / chart / copilot / grim / llm_audit / llm_enhance /
  multi_paper / pwa / stream` 等。改为 `for path in *_test.py` 自动发现 +
  真调脚本黑名单 + 「运行数 < 30 则报错」自检，杜绝清单再次腐烂。
- **`security_guard_test` 自身免疫环境投毒**：脚本内 `os.environ.pop`
  掉外部误设的 `RATE_LIMIT_DISABLE`（此前它在批处理里被全局变量坑成 52/4；
  单跑是 56/0。该套件测的就是限流器本身）。CI 的 `env:` 也删掉了这个全局变量。
- **真调脚本不再污染回归**：`kimi_mimo_live_test.py` 加 `--live` 显式门槛，
  不带则打印 SKIP 并 `exit 0`。此前它被 `*_test.py` 批扫到，必然 SIGTERM 超时，
  还会因上游额度/限流产生与代码无关的失败。
  同时修掉它文档里引用的不存在的 `kimi_mimo_test.py`。
- 新增 `pwa_test.py`（57 项）。

### 修复
- 确认并清理了端口 5000 上的**残留旧进程**：Windows 允许多进程绑定同一端口，
  操作系统会在它们之间负载均衡，表现为"同一 URL 时好时坏"（此前 `/favicon.ico`
  的间歇 404 即由此而来）。已终止孤儿进程并验证单进程下全部 PWA 端点 200。

---

## v2.5 — CLI 跨端与可复现性修复

### 新增
- **`cli.py` 命令行外壳**：`analyze` / `audit` / `simulate` / `check-data` / `methods`
  五个子命令，走与网页端完全相同的纯函数链路，结果逐字节一致。CLI 默认不写文件、
  只打印（`--output` 才落盘），红线自检同样生效。配套 `cli_test.py`（69 项）。
  `check-data` 可作 CI 门控（发现高优先级数据问题返回退出码 1，`--exit-zero` 可关闭）。

### 修复
- **`cli.py audit` 此前从未可用**：引用了不存在的 `audit_paper`。
  现接回 `build_audit_report`，并复用 `extract_paper.read_paper_text`
  （`.docx / .pdf / .txt / .md` 全支持）、`app._summarize_column`。
- **报告不可复现（重要）**：`extract_quantities` 用 `str.__hash__()` 当排序键，
  而字符串 hash 受 `PYTHONHASHSEED` 随机化影响 → 同一篇论文每次运行报告行序都不同。
  改为按匹配位置排序。对「统计可复现核查」工具而言，这是可信度问题。
- CLI 红线拦截路径不再外漏 `AttributeError` 堆栈，并输出正确用法指引。
- 字段缺失时给出「需要哪些字段 / 当前有哪些列 / 参数怎么对应」而非一句原始错误。

### 测试维护
- 修掉三处**快照式过期断言**（`compat_agent_test` / `router_cooldown_test`）：
  v1.8 新增 `kimi` / `mimo` 平台后，`== {5 个平台}` 之类断言误报。
  改为契约断言（核心平台存在、免费档以 MaaS 收尾）。

---

## v2.1 — 数据体检全链路

### 新增
- **`datacheck.py`**（产品入口第一站）：9 个检测器 + `grim_check()`，
  纯本地规则、零 LLM、零新依赖。检出：合计≠分项 / 取值越界 / 计数列小数 /
  重复行 / 前后测差值规律 / 常数列 / 反向题漏反向计分 / 直线作答 / 缺失模式。
- **一键清洗副本**（第二站）：`propose_fix(df, report)` + `POST /api/datacheck/fix`。
  只自动修「语义无歧义」的两类（重复行、反向题反向计分），**绝不修改原文件**。
  去重延后到最后统一执行，保证 `actions[].rows` 始终是原始 Excel 行号。
- 前端 Tab1 **上传即体检**（可折叠报告卡片 + 清洗按钮）。

### 修复（体检过程中暴露的真实缺陷）
- GRIM 公式漏乘 `n`（应校验 `n × mean` 是否为题项数的整数倍，而非看小数位）。
- 小样本越界漏报：原用「多数一致≥90%」判据，6 行数据里 1 个越界值报不出来。
  改为「中位数落在合理区间 → 任何越界值都可疑」，对 n 不敏感。
- 窄表假重复：粗粒度去重把 `two_way_data.csv` 误判出 46 行「重复」。
  改用可解释判据（必须存在「近似唯一列」）后，5 份内置示例 0 误报。

### 测试
- `datacheck_test.py`（77 项）、`datacheck_ui_test.py`（25 项）。

---

## v2.0 — 产品主线重构

### 新增
- **《专属桌面端开发总纲.md》**：把「数据体检」提升为整个产品的入口，
  确立八站流程图（上传 → 数据体检 → 清洗 → 方法推荐 → 统计计算 →
  人话解读 → 论文核查 → 写作/Word → 答辩准备）。
- 关键洞察：③–⑦ 站早已实装，①② 是新增入口 —— **换一个入口就换了一个产品**。

---

## v1.8 — 平台扩展

- 新增 **Kimi（月之暗面）** 与 **小米 MiMo** 两个平台（付费档 BYOK 尾部备胎，
  故意排在免费候选之后，免费用户零感知；`set_user_key()` 过的平台自动提到链首）。
- 平台总数达 7 家：`sf / zhipu / deepseek / dashscope / maas / kimi / mimo`。

---

## v1.7 — 人味增强

- **`tone_guide.py`** 语气规约：7 条可判定禁令（拆长句 / 删空泛总结 / 不堆排比 /
  结论句必须带统计量与样本量 / 少用套话 / 术语一致 / 不写小标题式废话），
  外加「建议必须可执行」审稿版。
- `AI_SPEAK_PATTERNS` + `find_ai_speak()`：规则化体检，**只报不改**。
- 注入两条冻结 prompt，并按契约 bump `PROMPT_VERSION` 到 v3。
- 定位到的真问题：原 prompt 里「避免 AI 腔」是句没有可判定标准的空话；
  而其余 8 个内置报告模板经检测**本身零 AI 腔**，无需改写。

---

## v1.6 — 阅读导航与审计对话

- **结果页阅读导航**（`ReadingNav`）：三个 Tab 的结果区统一支持目录跳读；
  `scan()` 同时识别 markdown 标题与面板自绘的 `[data-sec]` 小标题。
  护栏：标题少于 3 个不出目录，短报告零打扰。
- **②审计对话**：对审计中的**一条比对**追问。LLM 只解释、绝不计算；
  输入用白名单构造（不用 `**summary` 展开），无 Key 时回退规则引擎的 `verdict`
  模板，永远有话说、不白屏。提问本身也过红线检查。

---

## v1.5 — 学术红线自检

- **红线引擎**：`core` 类（代写 / 买卖 / 规避查重 / 伪造数据）无条件拦截；
  `writing` 类在有明确润色意图时可豁免。后端在 `/api/check_paper` 拦指令，
  前端在 `/api/red_line_scan` 提示。
- **③人话解释卡片（XAI）**：结构化 `explanations` 与旧 `suggestions: list[str]`
  并行输出，`fix` 字段必须指向真实存在的方法 key。

---

## v1.2 — 统计方法补齐

- **重复测量方差分析**：被试内设计、Mauchly 球形度检验 + Greenhouse-Geisser 校正、
  Bonferroni 事后比较。
- **论文副驾驶六阶段流水线**：资料调研 → 文献综述 → 研究设计 → 数据分析 →
  论文核查 → 论文撰写；阶段产出契约 + 顺序门控 + 断点恢复。
- **证据约束写作引擎**：claim 台账（来源 / 基线 / 置信度）+ 写作闸门自检
  （无来源不进正文、非 high 不进摘要、大词必须有数字）。
- LaTeX 与中文学位论文双模板。

---

## v1.1 — 量表与信度

- **Cronbach's α 信度分析**（α、删项后 α、CITC、信度等级建议）。
- **双因素方差分析**（Type III 平方和、主效应 + 交互效应）。
- **分步引导向导**，Likert 1–5 量表列识别。
- 固化「新增统计方法需同步修改 N 处」的约定。

---

## v1.0 — 回归与首个公开版本

- **多元线性回归**（OLS、R²、调整 R²、F 检验、系数 t 检验 + 95% CI、VIF）。
- **二元 Logistic 回归**（IRLS、Wald z、OR 值、McFadden 伪 R²）。
- 首个公开版本。

---

## v0.9 — 导出与文本提取

- **导出 Word**：标题 / 表格 / 列表自动排版 + 图表嵌入 + 封面元信息。
- `.pdf` 论文文本提取（含知网/万方 AES 加密 PDF 的空密码解密）。

---

## v0.8 — 流式渲染

- SSE 事件流（阶段消息 + 进度条 + 先文后图），`stream=0` 保留非流式兼容。

---

## v0.7 — 非参数方法

- **配对样本 T 检验**、**Mann-Whitney U**、**Wilcoxon 符号秩检验**（含效应量）。

---

## v0.5 — 多平台 LLM 路由

- 多平台容灾链 + 结果缓存 + 前缀缓存 + 失败静默降级。
- 免安装桌面版（PyInstaller）。
- 论文排查支持自然语言指令过滤（「只看 T 检验 / p<0.05 / 男组」）。

---

## v0.4 及更早

- **独立样本 T 检验**、**单因素 ANOVA**、**Pearson 相关**、**卡方检验**，
  以及列类型自动识别与推荐方法。
