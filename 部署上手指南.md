# 部署上手指南

> 面向：想把这个工具**开放给 30 人以内的同学**使用的你（本科在读、预算 ≤300 元）。
> 目标：**当天上线**，不备案，一年花费 ¥160–370。
> 全程只需要复制粘贴命令，不需要懂运维。

---

## 0. 先搞清三件事（决定你的所有选择）

### 0.1 域名不用审批，ICP 备案才是要等的那一步

很多人（包括一些 AI）会把这两件事混为一谈：

| 事项 | 要不要审批 | 耗时 | 花钱 |
| --- | --- | --- | --- |
| 买域名 | ❌ 不用 | 即时 | ¥60–80/年 |
| 域名实名认证 | ✅ 但很快 | 1–3 天 | 0 |
| **ICP 备案** | ✅ **必须** | **7–20 工作日** | 0 |
| 绑定国内服务器 | ✅ 必须已备案 | 同上 | — |

**关键点：备案是"中国大陆服务器"的准入条件，不是域名的。**
你买国外（含中国香港）服务器，**一行备案都不用做**，买完就能用。

> 我说的"三个月"是指最坏情况（材料被打回重提交 + 各省管局排队）。
> 顺利的话 7–20 个工作日，但确实**不能当天上线**。

### 0.2 你的工具必须跑 Python 后端，不能纯静态托管

我核实过代码：统计计算（`scipy` / `pandas` / `matplotlib`）全在 Python 端，
前端只是壳。所以：

- ❌ 不能丢到 GitHub Pages / Vercel 静态托管
- ✅ 必须有一台能跑 Python 的机器（VPS / 容器）

### 0.3 你现在没有登录系统，所以必须加访问口令

`app.py` 里用户数据存在内存字典 `_SESSION`，**没有任何账号/权限**。
网址一旦被转发，陌生人就能用。**必须开访问口令**（本指南第 3 步，一个环境变量搞定）。

---

## 1. 下单清单（照着买）

| # | 买什么 | 去哪买 | 选什么 | 价格 |
| --- | --- | --- | --- | --- |
| 1 | **轻量应用服务器** | 腾讯云 / 阿里云 官网 | 地域：**中国香港**；1核2G / 40GB SSD / 1000GB 流量；系统 **Ubuntu 22.04** | ¥99–288/年 |
| 2 | **域名** | Cloudflare Registrar（推荐）或 Namecheap | `.com` 或 `.cn` | ¥60–80/年 |
| 3 | **CDN + HTTPS** | Cloudflare 免费版 | 免费套餐 | ¥0 |
| 4 | **SSL 证书** | Let's Encrypt | 免费自动续期 | ¥0 |

**合计：首年 ¥160–370，一次付清。**

### ⚠️ 必须做的两件事

**① 学生认证**：在腾讯云/阿里云后台找「学生认证」（学信网验证），
认证后香港轻量服务器能低到 **¥99/年** 甚至更低。**这一项省的钱最多，务必先做。**

**② 地域一定要选"中国香港"**，别选欧美：

| 地域 | 国内访问延迟 | 体验 |
| --- | --- | --- |
| **中国香港** | 30–60ms | 几乎无感，**推荐** |
| 新加坡 / 日本 | 60–120ms | 可接受 |
| 美国 / 欧洲 | 150–300ms | 晚高峰明显卡 |

---

## 2. 服务器初始化（约 10 分钟）

买完服务器后会拿到：**公网 IP**、**root 密码**（或密钥）。

### 2.1 登录服务器

```bash
# 在你自己的电脑上打开终端（Windows 用 PowerShell 或 Git Bash）
ssh root@你的服务器IP
# 首次会问 yes/no，输 yes；然后输密码（输入时不显示，正常）
```

### 2.2 装 Docker（两条命令）

```bash
# 一键安装脚本（官方）
curl -fsSL https://get.docker.com | sh

# 验证
docker --version
docker compose version
```

看到版本号就成功了。

### 2.3 装 Nginx（用于反代 + 自动证书）

```bash
apt update && apt install -y nginx certbot python3-certbot-nginx git
```

### 2.4 放行端口

在**云服务商控制台**的「防火墙 / 安全组」里，放行：

- `22`（SSH，装完可改成只允许你的 IP）
- `80`（HTTP）
- `443`（HTTPS）

> ⚠️ 这是最容易漏的一步。不开 80/443，外面永远访问不到。

---

## 3. 加访问口令（**上线前必做，一个变量搞定**）

你的工具没有账号系统，所以这一步不能省。

> **上线门禁三件事**（2026-09 安全自查后的口径）：①`ACCESS_CODE` 开门禁；
> ②LLM 接口限流 `RATE_LIMIT_LLM_PER_MIN=8`（**不设也默认开启**，怕误伤可调，怕刷可收紧到 4）；
> ③`ACCESS_COOKIE_SECURE=1`（**配好 HTTPS 再开**，HTTP 下开了会登录死循环）。
> 三者缺一，等于有一道门没装。

### 3.1 口令怎么设

就是一个环境变量 `ACCESS_CODE`：

```bash
ACCESS_CODE=你的口令
```

**设了才有门禁，不设则完全开放**（本地双击桌面端、跑测试的行为不变）。

### 3.2 口令怎么选

- ✅ 中英文混合 + 数字，长度 ≥12：`论文核查-2026-abc`
- ❌ 别用 `123456`、`admin`、你的学号
- ✅ 中文完全没问题（我专门修了中文口令会崩的 bug 并加了测试）

### 3.3 你怎么把口令给同学

微信群发一句话，或贴给班级群。**别写在公开网页上。**

### 3.4 改口令 = 所有人立即要重新输

唯一要记住的运维动作：改 `ACCESS_CODE` 后 `docker compose restart`，
**所有旧 cookie 立即失效**。万一口令泄露，这是你的逃生手段。

---

## 4. 部署应用（约 10 分钟）

### 4.1 上传代码

```bash
# 在服务器上
cd /opt
git clone https://github.com/wangzhiguan72-droid/zhilun-assistant.git zhilun
cd zhilun
```

> 公开仓库直接 clone 即可，**不需要在服务器上登录 GitHub**。
> 如果你把仓库设成了 Private，就需要 Personal Access Token：
> `git clone https://<用户名>:<PAT>@github.com/wangzhiguan72-droid/zhilun-assistant.git zhilun`
> （PAT 会留在服务器 shell 历史里，用 `git config credential.helper store` 更规范。）

### 4.2 建环境变量文件

```bash
cat > .env <<'EOF'
# ===== 访问口令（必填！不填则任何人可访问）=====
ACCESS_CODE=换成你自己的口令

# ===== 生产配置 =====
HOST=0.0.0.0
PORT=5000
DEBUG=0

# LLM 接口单独限流（默认 8 次/分/IP；怕被刷可收紧到 4。绝不设 0，0 会被回退为默认）
RATE_LIMIT_LLM_PER_MIN=8

# 配好 HTTPS 后改 1，让 cookie 只在 HTTPS 下传输
ACCESS_COOKIE_SECURE=0

# 反代后面拿真实 IP（用 Nginx 时必须开，否则限流会把所有人算成同一个 IP）
# ⚠️ 只在 Nginx/Cloudflare 反代后面才设 1；直连公网千万别设——
#    否则任何人伪造 X-Forwarded-For 就能换 IP 绕过限流
TRUST_PROXY=1

# ===== LLM（可选，不填则 AI 功能自动降级，其余功能完整可用）=====
# 按需填一家即可
# DEEPSEEK_API_KEY=sk-xxx
EOF

# 保护配置（含口令，别让别人读到）
chmod 600 .env
```

### 4.3 启动

```bash
docker compose up -d

# 看日志确认起来了
docker compose logs -f --tail=50
```

看到 `Listening at: http://0.0.0.0:5000` 就成功了。按 `Ctrl+C` 退出日志（不影响运行）。

### 4.4 本机自测（还在服务器上）

```bash
# 健康检查（这个接口不需要口令，Docker 也用它）
curl http://127.0.0.1:5000/health

# 试着访问首页 —— 应该看到登录页（说明口令生效了）
curl -s http://127.0.0.1:5000/ | head -20
```

**看到登录页 HTML = 口令生效。** 如果直接看到主页内容，说明 `.env` 里的
`ACCESS_CODE` 没生效，检查拼写后 `docker compose restart`。

---

## 5. 域名 + HTTPS + Cloudflare（约 20 分钟）

### 5.1 域名解析到服务器

如果域名买在 Cloudflare（推荐，最省事）：

1. 登录 Cloudflare → 选你的域名 → **DNS**
2. 添加记录：
   - 类型：`A`
   - 名称：`@`（代表主域名）
   - IPv4 地址：**你的服务器 IP**
   - **代理状态：橙色云朵（已代理）** ← 重要，这才能加速
3. 再加一条 `A` 记录，名称 `www`，同样指向服务器 IP

如果域名买在别处（Namecheap 等）：

1. 先把域名的 **Nameserver** 改成 Cloudflare 给的两个地址
2. 等生效（几分钟到几小时）
3. 再按上面的步骤加 A 记录

### 5.2 给 Nginx 写配置

**先把 Cloudflare 的 SSL 模式设成 Full，再配 Nginx**，否则会死循环：

> Cloudflare → SSL/TLS → Overview → 模式选 **Full**（不是 Flexible！）

```bash
cat > /etc/nginx/sites-available/zhilun <<'EOF'
server {
    listen 80;
    server_name 你的域名.com www.你的域名.com;

    client_max_body_size 25M;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 统计计算可能较慢，放宽超时
        proxy_read_timeout 180s;
        proxy_send_timeout 180s;
        proxy_buffering off;
    }
}
EOF

ln -sf /etc/nginx/sites-available/zhilun /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t          # 测试配置语法
systemctl reload nginx
```

### 5.3 申请免费 HTTPS 证书（自动续期）

```bash
certbot --nginx -d 你的域名.com -d www.你的域名.com
```

按提示：
- 输入邮箱（收证书到期提醒）
- 同意条款输 `A`
- 是否订阅邮件输 `N`
- 是否把 HTTP 重定向到 HTTPS → **选 2（重定向）**

完成后自动改好 Nginx 配置并**自动续期**，你什么都不用管。

### 5.4 用自签证书让 Cloudflare 回源加密（推荐）

Cloudflare 的 Full 模式需要一个源站证书。用 Let's Encrypt 的那张即可，
但如果想让 Cloudflare 完全信任，可用 Cloudflare 生成的 Origin Certificate
（Cloudflare → SSL/TLS → Origin Server → Create Certificate），
把证书内容贴到服务器，然后在 Nginx 配置里替换 `ssl_certificate` 两行。

**不想折腾可以先跳过**，Full 模式用 Let's Encrypt 证书也能正常工作。

### 5.5 打开 Cloudflare 加速开关

Cloudflare → 你的域名，这几项建议打开：

| 项目 | 位置 | 建议 |
| --- | --- | --- |
| Auto Minify | Speed → Optimization | ✅ 全勾（HTML/CSS/JS） |
| Brotli | Speed → Optimization | ✅ 开 |
| Rocket Loader | Speed → Optimization | ✅ 开 |
| Always Use HTTPS | SSL/TLS → Edge Certificates | ✅ 开 |
| Automatic HTTPS Rewrites | 同上 | ✅ 开 |
| Minimum TLS Version | 同上 | 1.2 |

---

## 6. 验收清单（上线前逐条打勾）

在**手机 4G/5G**（不要连 WiFi）上访问你的域名，逐条确认：

- [ ] 打开域名 → 看到**登录页**（不是直接进主页）
- [ ] 输错口令 → 提示"口令不正确"
- [ ] 输对口令 → 进入主页
- [ ] 上传一个 CSV → 能正常出体检报告
- [ ] 刷新页面 → **不用重新输口令**（cookie 生效）
- [ ] 关掉浏览器重开 → 仍不用重输
- [ ] 点访问 `/logout` → 回到登录页
- [ ] 再访问域名 → 又要输口令

全部通过就**可以发给同学了**。

---

## 7. 发给同学的模板（直接复制）

> 【论文数据核查工具】内测邀请
>
> 网址：https://你的域名.com
> 访问口令：xxxxx（进群的同学都能用）
>
> 能做什么：上传你的问卷/实验数据 CSV，自动查数据矛盾、跑统计、
> 生成论文里该写的统计段落。
>
> 注意：
> 1. **数据只在你上传的那次会话里处理，不落盘**，但请不要传含姓名、
>    身份证号等敏感信息的原始表
> 2. 这是内测版，遇到问题欢迎反馈
> 3. 口令请勿外传

---

## 8. 日常维护（每月花 5 分钟）

### 8.1 看状态

```bash
cd /opt/zhilun

docker compose ps              # 容器是否在跑
docker compose logs --tail=100 # 最近日志
df -h                          # 磁盘是否快满
free -h                        # 内存
```

### 8.2 更新代码

```bash
cd /opt/zhilun
git pull
docker compose up -d --build   # 重新构建并重启
```

### 8.3 改口令

```bash
cd /opt/zhilun
nano .env                      # 改 ACCESS_CODE
docker compose restart
```

### 8.4 备份

定期把服务器上的 `.env`（含口令）抄一份到本地。代码在 GitHub 上，不怕丢。

### 8.5 证书续期

`certbot` 装了定时任务**自动续期**，一般不用管。想手动检查：

```bash
certbot renew --dry-run
```

---

## 9. 出问题了怎么办（按症状查）

| 症状 | 原因 | 解决 |
| --- | --- | --- |
| 域名打不开，一直转圈 | 防火墙没放 80/443 | 云控制台安全组放行 |
| **输对口令却一直回登录页** | ①`ACCESS_COOKIE_SECURE=1` 但你在用 HTTP ②浏览器禁了 cookie | 先改 `ACCESS_COOKIE_SECURE=0`；换浏览器试 |
| 页面报"重定向次数过多" | Cloudflare SSL 设成了 Flexible | 改成 **Full** |
| 502 Bad Gateway | 容器没起来或崩了 | `docker compose logs --tail=100` 看报错 |
| 同学说很慢 | Cloudflare 免费线路晚高峰波动 | 见第 11 节升级方案 |
| 所有人都提示"请求过于频繁" | 没开 `TRUST_PROXY=1`，限流把所有访问算成同一个 IP | `.env` 里设 `TRUST_PROXY=1`，重启 |
| 个别人用 AI 功能报 429"请求过于频繁" | 触发 LLM 接口独立限流（8 次/分/IP）；校园网 NAT 下同一出口 IP 共享额度 | 属正常防护；确系误伤可放宽 `RATE_LIMIT_LLM_PER_MIN`，或引导错峰使用 |
| 上传大文件报 413 | Nginx 的 `client_max_body_size` | 第 5.2 节配置里已设 25M |

### 看日志的正确姿势

```bash
# 实时看
docker compose logs -f

# 只看最近 200 行
docker compose logs --tail=200

# 找错误
docker compose logs | grep -i error
```

---

## 10. 安全边界（**务必诚实告知使用者**）

这套方案**只防"网址被转发给无关的人"**，它不是防攻击系统：

| 能做到 | 做不到 |
| --- | --- |
| 陌生人没口令进不来 | 防定向攻击：口令爆破只有 60 次/分的通用限流、无独立账号锁定——**口令设 8 位以上中英混合后，60 次/分钟等于无效攻击**；真被盯上就套 Cloudflare（免费 WAF + DDoS 防护） |
| 口令不写在 cookie 里（只存 HMAC-SHA256 摘要，常量时间比较） | 没有"找回口令"、"记住设备"等功能 |
| 改口令可让所有旧 cookie 立即失效 | **没有逐个用户的权限控制**（限额按 IP 不按人：校园网 NAT 下多人共享额度；换代理池可绕过单 IP 限流） |
| LLM 接口单独限流（8 次/分/IP），且**出厂默认只走零成本免费模型白名单**（不设 `LLM_TIER=pro` 谁也碰不到付费模型）——被刷也刷不爆你的钱包 | 没有审计日志（谁上传了什么，查不到） |
| BYOK：同学填的**自带 Key 请求级隔离**（v2.25），服务端零残留、用户之间不串号 | 分享链接 `/s/<token>` 在门禁**外**（设计如此：给没口令的导师看）——别把含敏感数据的报告随手分享 |

**数据流向要如实说明**：数据只在你服务器的内存里处理，
**不落盘、不发第三方**（除非你配了 LLM Key，那时数据分析片段会发给模型厂商）。

> ⚠️ 配了 LLM Key 的情况下：论文片段/统计结果会发送给模型厂商用于生成解读。
> 这一点在邀请同学时最好提一句。

**给同学的建议**：别上传含姓名、身份证号等敏感信息的原始问卷表。

### 10.1 协作审阅链接（v2.16）：**绕过口令，这是有意的**

核查报告可以生成 `/s/<token>` 分享链接，而 **`/s/` 前缀对访问口令放行** ——
因为分享的初衷就是给**没有口令的导师 / 同门**看。所以：

| 事实 | 你要知道 |
| --- | --- |
| 拿到链接就能看那份报告 | token 是 96 bit 随机串，**猜不出来**；但**转发即扩散**，没有收回按钮 |
| 对方能追加批注 | 只能**追加**，改不了报告正文（这是硬约束，测试逐字节守着） |
| 内容落在服务器 `.review_share/` | 默认 **30 天过期**自动清理；想立刻销毁就删那个目录下的对应文件 |
| 链接里不含原始数据 | 只有报告文字与统计量，**不含上传的 CSV/Excel** |

**收回链接的正确做法**：删掉 `容器或项目内 .review_share/<token>.json`，
链接立即 404。或者干脆 `export REVIEW_SHARE_DIR=off` 关闭整个功能。

运维有两个小接口（**都在口令门禁内**）：`GET /api/review/list?limit=50` 列出
分享元信息（标题 / 时间 / 批注数 / 是否过期，**不含报告正文**），可用来盘点
"发出去多少条"；`POST /api/review/gc` 立刻清掉过期的，不必等 30 天——
想做成定时任务就 `0 3 * * * curl -X POST http://127.0.0.1:5000/api/review/gc`。

> 如果你希望"分享也必须输口令"，把 `app.py` 里 `access_guard._EXEMPT_PREFIXES`
> 的 `"/s/"` 去掉即可（功能仍在，只是对方得先过门禁）。

---

## 11. 如果速度还不够（备选升级）

Cloudflare 免费版在国内走国际线路，**晚高峰可能掉到 3–5 秒**。
如果这在你的"决定性条件"里不能接受：

| 方案 | 加价 | 效果 | 代价 |
| --- | --- | --- | --- |
| **国内服务器 + 备案** | ¥99/年起 | 首屏 0.5 秒 | 要等 7–20 工作日 |
| 香港服务器 + 国内 CDN | +¥50–100/年 | 首屏 1–2 秒 | 国内 CDN 也要备案 |
| 阿里云全球加速 | +¥200+/年 | 首屏 1 秒内 | 超预算 |

**我的建议**：先按本指南**今天上线**让同学用起来，等真的卡了再升级。
**先跑起来，比等审批重要得多。**

---

## 12. 进阶：桌面端（**不用买服务器、不用备案、不用域名**）

你说得对 —— 对"给人用"这件事，**桌面端确实比网页版简单得多**。

### 12.1 为什么桌面端更简单

| | 网页版 | 桌面端 |
| --- | --- | --- |
| 要买服务器 | ✅ 必须 | ❌ 不用 |
| 要买域名 | ✅ 必须 | ❌ 不用 |
| 要备案 | 国外不用 / 国内要 | ❌ 完全不用 |
| 要配 HTTPS | ✅ 必须 | ❌ 不用（本机 http://127.0.0.1） |
| 要设访问口令 | ✅ 必须 | ❌ 不用（只有本机能访问） |
| 上线耗时 | 半天 | **打包 3 分钟** |
| 别人怎么用 | 打开网址 | **下载 exe，双击** |

**核心原因**：桌面端把服务跑在**使用者自己的电脑上**，
不存在"公网暴露"这件事，所以**所有审批、证书、防护统统不需要**。

### 12.2 但它有一个硬伤

**桌面端没法"一个人部署、所有人用"**：

- 每个同学都要下载那个 exe（预计 200–400MB）
- 你更新了代码，**每个人都要重新下载**
- 他们的数据在他们自己电脑上，你收不到任何使用数据

**所以两种形态是互补的，不是二选一：**

| 用途 | 选哪个 |
| --- | --- |
| 给同学试用、收集反馈 | **桌面端**（发个 exe 到群里，最快） |
| 长期服务、收集使用数据、参赛展示 | **网页版**（一个网址，改完所有人自动同步） |

### 12.3 怎么打桌面端（你项目里已经有现成脚本）

```bash
# 在你自己的 Windows 电脑上
.venv\Scripts\python.exe build_desktop.py
```

产出在 `dist/智论助手.exe`，**单文件、免安装 Python、双击即用**。
它会自动打开浏览器指向 `http://127.0.0.1:5000`。

### 12.4 ⚠️ 桌面端分发的两个坑

**① Windows 会拦截未签名的 exe**

同学双击时可能出现「Windows 已保护你的电脑」蓝框。解决：
- 让同学点「更多信息」→「仍要运行」
- 或在邀请说明里写清楚这一步，否则很多人会以为有毒

**② 代码签名证书很贵**（¥1000+/年），**你现在完全不用买**。
小范围内部使用，口头说明即可。

### 12.5 我的实际建议

**两条腿走**：
1. **今天就**打包 exe 发群里，让 3–5 个同学先用起来（零成本、零等待）
2. **同时**按第 1–6 步部署网页版，一周内上线（¥160–370）

网页版还能帮你**收集真实使用数据** —— 这对参加 2026 大创赛的
「项目创新」和「产业价值」评分是**实打实的证据**，桌面端给不了。

---

## 13. 进度清单（代码侧已完成 / 你要自己做的）

### ✅ 代码侧已就绪

| 项 | 状态 |
| --- | --- |
| **GitHub 提交** | ✅ 已提交 116 个文件 / 23852 行（commit `563c5f9`），作者 `tiandaozongsi` |
| **LICENSE 实名** | ✅ `Copyright (c) 2026 tiandaozongsi`（MIT） |
| **访问口令** | ✅ `access_guard.py` + 92 项测试，一行环境变量 `ACCESS_CODE` 控制 |
| **桌面端 exe** | ✅ `python build_desktop.py` 一键出 `dist/智论助手.exe` |
| **部署配置** | ✅ `Dockerfile` / `docker-compose.yml` / `wsgi.py` / `gunicorn.conf.py` |

### ⚠️ 只有你能做的三件事

1. **在 GitHub 建空仓库并 push** —— 去 <https://github.com/new>
   - 仓库名：`zhilun-assistant`
   - **不要勾** README / .gitignore / LICENSE（会和本地冲突）
   - 建完在项目根目录跑本仓库自带的脚本：
     ```powershell
     powershell -ExecutionPolicy Bypass -File _push_github.ps1
     ```
     首次推送会要求登录 —— **密码处填 Personal Access Token**（不是账号密码）：
     <https://github.com/settings/tokens> → Generate new token (classic) → 勾 `repo` → 复制

2. **买服务器 + 域名** —— 见第 1 节下单清单（记得先做学生认证）

3. **报名 2026 大创赛** —— 9/14–9/25 窗口，只有你能操作

---

## 附：一页速查卡

```bash
# ===== 首次部署 =====
ssh root@服务器IP
curl -fsSL https://get.docker.com | sh
apt update && apt install -y nginx certbot python3-certbot-nginx git
cd /opt && git clone https://github.com/wangzhiguan72-droid/zhilun-assistant.git zhilun && cd zhilun
cat > .env <<'EOF'
ACCESS_CODE=你的口令
HOST=0.0.0.0
PORT=5000
DEBUG=0
RATE_LIMIT_LLM_PER_MIN=8
ACCESS_COOKIE_SECURE=0
TRUST_PROXY=1
EOF
chmod 600 .env
docker compose up -d
# → Nginx 配置（见 5.2）→ certbot --nginx -d 域名（见 5.3）
# → Cloudflare SSL 设 Full、开小云朵（见 5.1 / 5.5）

# ===== 日常 =====
cd /opt/zhilun
docker compose ps                  # 状态
docker compose logs --tail=100     # 日志
git pull && docker compose up -d --build   # 更新
docker compose restart             # 改完 .env 重启
```
